# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1518.001 - Security Software Enumeration (AV/EDR process and service discovery) (Security product enumeration)
id: 6102a585-2a27-55ce-be31-ac65a2ae9533
status: experimental
description: 'Execution of commands specifically targeting security product processes and services (MsMpEng, CrowdStrike, SentinelOne, Sophos, Tanium, Splunk forwarders) to assess which defenses to evade or disable Query block: Security product enumeration.'
references:
    - MITRE ATT&CK T1518.001
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1518.001
    - attack.g0096
    - attack.g0016
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name:
            - tasklist.exe
            - wmic.exe
            - sc.exe
        process.command_line:
            - '*AntiVirus*'
            - '*falcon*'
            - '*SentinelOne*'
            - '*crowdstrike*'
            - '*Sophos*'
            - '*splunk*'
            - '*ossec*'
            - '*wazuh*'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1518.001
    technique_name: 'Software Discovery: Security Software Discovery'
    os: win
    query_block: 1 of 2
    original_query: |-
        process.name: ("tasklist.exe" OR "wmic.exe" OR "sc.exe")
        AND process.command_line: (*AntiVirus* OR *falcon* OR *SentinelOne*
          OR *crowdstrike* OR *Sophos* OR *splunk* OR *ossec* OR *wazuh*)
    sysmon: |-
        // Sysmon EID 1 - security-targeted tasklist/wmic
        Image=(*\\tasklist.exe OR *\\wmic.exe)
        CommandLine=(*AntiVirus* OR *MsMpEng* OR *csfalcon* OR *SentinelOne*
          OR *CylanceSvc* OR *Sophos* OR *crowdstrike* OR *Tanium*
          OR *splunkd* OR *winlogbeat* OR *ossec* OR *wazuh*)

        // PowerShell security product query
        CommandLine=(*Get-MpComputerStatus* OR *SecurityCenter2* OR *AntiVirusProduct*)

        // Linux: ps aux | grep falcon/ossec/wazuh/auditd/sysmon
    powershell: |-
        # Security software discovery detection
        Write-Host "[*] === Security product queries ==="
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[10].Value -match 'AntiVirus|MsMpEng|falcon|SentinelOne|Sophos|crowdstrike|SecurityCenter|Get-MpComputerStatus' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: |-
        Security software registered at:
          HKLM\SOFTWARE\Microsoft\Security Center\Provider\Av
          WMI: root\SecurityCenter2\AntiVirusProduct
    ossdetect: |-
        Sigma:
        - win_proc_creation_security_software_discovery.yml
        Elastic:
        - Security Software Discovery
    tools: |-
        Sysmon (EID 1)
        PowerShell ScriptBlock logging
        Auditd (Linux)
    actors:
        - APT41
        - APT29
    analyst_notes: 'Security discovery followed by Set-MpPreference exclusions or service stops is an active attack chain, not just reconnaissance. The attacker''s next action depends on what they find: Defender only means AMSI bypass; CrowdStrike means driver-level evasion needed; no EDR means unrestricted operations.'
