# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1049 - Active Connection Enumeration (netstat, ss, Get-NetTCPConnection, lsof) (Linux)
id: c248e4c1-9f53-576d-875d-938a7d3b8572
status: experimental
description: 'Execution of netstat -ano, ss -tunlp, or Get-NetTCPConnection to enumerate active connections, listening ports, and associated PIDs for lateral movement targeting and network mapping Query block: Linux.'
references:
    - MITRE ATT&CK T1049
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1049
    - attack.g0096
    - attack.g1017
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name:
            - ss
            - netstat
        process.args:
            - -tunlp
            - -ano
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1049
    technique_name: System Network Connections Discovery
    os: linux
    query_block: 3 of 3
    original_query: |-
        process.name: ("ss" OR "netstat")
        AND process.args: ("-tunlp" OR "-ano")
    sysmon: |-
        // Windows: Image=*\\netstat.exe AND CommandLine=(*-ano* OR *-b*)
        // PowerShell: CommandLine=*Get-NetTCPConnection*

        // Linux - auditd rules
        -a always,exit -F arch=b64 -S execve -F exe=/usr/bin/ss -k net_discovery
        -a always,exit -F arch=b64 -S execve -F exe=/usr/bin/netstat -k net_discovery

        // Linux: Image=(*/ss OR */netstat) AND CommandLine=(*-tunlp* OR *-ano*)
        // Image=*/lsof AND CommandLine=*-i*
    powershell: |-
        # Connection discovery detection
        # Windows:
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[4].Value -match 'netstat' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,150)}}

        # Linux: ausearch -k net_discovery -ts recent
    registry: No registry artifact.
    ossdetect: |-
        Sigma:
        - win_proc_creation_netstat.yml
        Elastic:
        - Network Connection Discovery
    tools: |-
        Sysmon (EID 1)
        Auditd (Linux)
        osquery: SELECT * FROM listening_ports; SELECT * FROM process_open_sockets;
    actors:
        - APT41
        - Volt Typhoon
    analyst_notes: Distinct from T1016 (config discovery). Config tells the attacker what networks are REACHABLE; connection discovery tells them what hosts are actively TALKING to this one. Established connections reveal already-authenticated paths. ss -tunlp is preferred over netstat on Linux for speed and reliable PID mapping.
