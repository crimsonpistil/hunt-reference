# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1057 - Process Listing (tasklist, Get-Process, ps aux, wmic process) (Linux)
id: 50a56a22-af06-5761-9fcd-dbe0f8811f9b
status: experimental
description: 'Execution of tasklist, Get-Process, wmic process list, or ps aux to enumerate running processes for security tool identification, injection target selection, and token theft opportunities Query block: Linux.'
references:
    - MITRE ATT&CK T1057
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1057
    - attack.g0096
    - attack.g0016
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name: ps
        process.args:
            - aux
            - -ef
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1057
    technique_name: Process Discovery
    os: win
    query_block: 2 of 2
    original_query: |-
        process.name: "ps"
        AND process.args: ("aux" OR "-ef")
    sysmon: |-
        // Sysmon EID 1 - process enumeration
        Image=(*\\tasklist.exe)
        Image=*\\wmic.exe AND CommandLine=*process*list*
        CommandLine=(*Get-Process* OR *Get-WmiObject*Win32_Process*)

        // Linux: Image=(*/ps) AND CommandLine=(*aux* OR *-ef*)
        // Output redirect (saving results): CommandLine=(*tasklist*>* OR *ps*>*)
    powershell: |-
        # Process discovery detection
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[4].Value -match 'tasklist' -or
            $_.Properties[10].Value -match 'Get-Process|Win32_Process' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,150)}}
    registry: No registry artifact.
    ossdetect: |-
        Sigma:
        - win_proc_creation_tasklist.yml
        Elastic:
        - Process Discovery via Tasklist
    tools: |-
        Sysmon (EID 1)
        Auditd (Linux)
    actors:
        - APT41
        - APT29
    analyst_notes: 'Low-confidence individually but high-value as a sequence component. An attacker uses tasklist to find: security tools to evade, browsers with saved creds, processes running as other users for token theft, and suitable injection targets. The piped-to-file variant (tasklist > procs.txt) indicates staging for exfil, which is higher-confidence.'
