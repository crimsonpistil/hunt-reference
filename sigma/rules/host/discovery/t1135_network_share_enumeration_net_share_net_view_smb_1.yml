# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1135 - Network Share Enumeration (net share, net view, smbclient, Get-SmbShare) (PowerShell)
id: 70251838-8eba-5783-b077-931f38b65cfe
status: experimental
description: 'Execution of net share, net view, Get-SmbShare, or smbclient -L to enumerate local and remote network shares for lateral movement path identification and sensitive data discovery Query block: PowerShell.'
references:
    - MITRE ATT&CK T1135
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1135
    - attack.g0096
    - attack.g0016
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.command_line:
            - '*Get-SmbShare*'
            - '*Win32_Share*'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1135
    technique_name: Network Share Discovery
    os: win
    query_block: 2 of 2
    original_query: 'process.command_line: (*Get-SmbShare* OR *Win32_Share*)'
    sysmon: |-
        // Sysmon EID 1 - share enumeration
        Image=(*\\net.exe OR *\\net1.exe)
        CommandLine=(*share* OR *view*)

        // PowerShell: CommandLine=(*Get-SmbShare* OR *Win32_Share*)
        // Linux: Image=*/smbclient AND CommandLine=*-L*
        // Linux: Image=*/showmount AND CommandLine=*-e*
    powershell: |-
        # Share discovery detection
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[10].Value -match 'net.*(share|view)|Get-SmbShare' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: |-
        Shares defined at:
          HKLM\SYSTEM\CurrentControlSet\Services\LanmanServer\Shares

        Default admin shares: C$, ADMIN$, IPC$ (always present on domain-joined Windows).
    ossdetect: |-
        Sigma:
        - win_proc_creation_net_share_discovery.yml
        Elastic:
        - Network Share Discovery
    tools: |-
        Sysmon (EID 1 + EID 3 SMB)
        Security EID 5140/5145 (share access audit)
        CrackMapExec (--shares)
        BloodHound
    actors:
        - APT41
        - APT29
    analyst_notes: Share discovery bridges account enumeration and lateral movement. The admin shares (C$, ADMIN$) are the paths PsExec and Impacket use. net view /all shows hidden shares. On Linux, showmount -e and smbclient -L are equivalents.
