# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1087.002 - Domain Account and Group Enumeration (net user/group /domain, nltest, ADFind) (ADFind)
id: 698a667d-7630-5eed-9ec2-58abcc5e453c
status: experimental
description: 'Execution of domain enumeration commands to map privileged accounts, group memberships, and trust relationships for credential targeting and lateral movement planning Query block: ADFind.'
references:
    - MITRE ATT&CK T1087.002
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1087.002
    - attack.g0016
    - attack.g0096
    - attack.g0032
    - attack.g0007
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name: adfind.exe
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1087.002
    technique_name: 'Account Discovery: Domain Account'
    os: win
    query_block: 4 of 5
    original_query: 'process.name: "adfind.exe"'
    sysmon: |-
        // Sysmon EID 1 - net.exe domain enumeration
        Image=(*\\net.exe OR *\\net1.exe)
        CommandLine=(*user*/domain* OR *group*/domain*
          OR *"Domain Admins"* OR *"Enterprise Admins"*)

        // nltest domain trust enumeration
        Image=*\\nltest.exe
        CommandLine=(*dclist* OR *domain_trusts* OR *dsgetdc*)

        // PowerShell AD cmdlets
        CommandLine=(*Get-ADUser* OR *Get-ADGroup* OR *Get-ADComputer*
          OR *Get-ADDomain* OR *[adsisearcher]*)

        // ADFind (ransomware pre-staging indicator)
        Image=*\\adfind.exe
    powershell: |-
        # Domain enumeration detection
        Write-Host "[*] === Domain enumeration commands ==="
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[10].Value -match 'net.*/domain|nltest|adfind|Get-AD' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: |-
        No registry artifact.

        LDAP queries generated by these commands are logged in Security EID 4662 on DCs if Directory Service Access audit is enabled.
    ossdetect: |-
        Sigma:
        - win_proc_creation_net_domain_enum.yml
        - win_proc_creation_nltest.yml
        - win_proc_creation_adfind.yml
        Elastic:
        - Domain Account Discovery
        - Active Directory Enumeration via nltest
    tools: |-
        Sysmon (EID 1)
        PowerShell ScriptBlock logging (EID 4104)
        Security EID 4661/4662 on DCs
        BloodHound / SharpHound
    actors:
        - APT29
        - APT41
        - Lazarus
        - APT28
    analyst_notes: 'ADFind is a strong ransomware pre-staging indicator: Conti, Ryuk, and BlackBasta playbooks all document ADFind as a required step. The combination of net group ''Domain Admins'' /domain followed by Kerberoasting or DCSync should be treated as a confirmed attack chain.'
