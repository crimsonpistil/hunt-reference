# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1016 - Network Config Enumeration (ipconfig, ifconfig, route, arp, ip addr) (Linux)
id: acea7a13-09c3-59c4-ba51-5e7154efc6f2
status: experimental
description: 'Execution of ipconfig /all, route print, arp -a, or Linux equivalents (ip addr, ip route, ss) to map local network topology, DNS infrastructure, and adjacent subnets for lateral movement planning Query block: Linux.'
references:
    - MITRE ATT&CK T1016
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1016
    - attack.g0096
    - attack.g1017
    - attack.g0016
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name:
            - ip
            - ifconfig
            - route
            - ss
            - netstat
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1016
    technique_name: System Network Configuration Discovery
    os: win
    query_block: 2 of 2
    original_query: 'process.name: ("ip" OR "ifconfig" OR "route" OR "ss" OR "netstat")'
    sysmon: |-
        // Sysmon EID 1 - Windows network config
        Image=(*\\ipconfig.exe OR *\\route.exe OR *\\arp.exe OR *\\netsh.exe)

        // Linux: Image=(*/ip OR */ifconfig OR */route OR */arp OR */ss)
    powershell: |-
        # Network config discovery detection
        Write-Host "[*] === Network discovery commands ==="
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[4].Value -match 'ipconfig|route\.exe|arp\.exe|netsh' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,150)}}
    registry: |-
        No registry artifact.

        Network config stored at:
          HKLM\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters\Interfaces
    ossdetect: |-
        Sigma:
        - win_proc_creation_network_discovery.yml
        Elastic:
        - Network Configuration Discovery
    tools: |-
        Sysmon (EID 1)
        Auditd (Linux)
    actors:
        - APT41
        - Volt Typhoon
        - APT29
    analyst_notes: 'The detection value is in the SEQUENCE: ipconfig alone is noise, but ipconfig + arp + route + net view within 60 seconds from a single process tree is a structured discovery burst. Detect the pattern, not the individual command.'
