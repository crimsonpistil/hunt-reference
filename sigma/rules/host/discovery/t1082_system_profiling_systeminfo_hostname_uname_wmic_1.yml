# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/discovery.js
title: T1082 - System Profiling (systeminfo, hostname, uname, wmic os) (Linux)
id: 7091f645-b587-56d8-a019-1a8ab0efd8c3
status: experimental
description: 'Execution of systeminfo, hostname, ver, wmic os get, or uname -a to profile the OS, patch level, domain membership, and hardware for exploit compatibility and targeting decisions Query block: Linux.'
references:
    - MITRE ATT&CK T1082
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1082
    - attack.g0096
    - attack.g0007
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name:
            - uname
            - hostnamectl
            - lsb_release
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/discovery.js
    domain: host
    technique: T1082
    technique_name: System Information Discovery
    os: win
    query_block: 2 of 2
    original_query: 'process.name: ("uname" OR "hostnamectl" OR "lsb_release")'
    sysmon: |-
        // Sysmon EID 1 - system profiling
        Image=(*\\systeminfo.exe OR *\\hostname.exe)
        Image=*\\wmic.exe AND CommandLine=(*os*get* OR *computersystem*get*)

        // PowerShell variants
        CommandLine=(*Get-ComputerInfo* OR *Win32_OperatingSystem*)

        // Linux: Image=(*/uname OR */hostnamectl OR */lsb_release)
    powershell: |-
        # System profiling detection
        Write-Host "[*] === systeminfo/hostname executions ==="
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -EA SilentlyContinue |
          Where-Object { $_.Properties[4].Value -match 'systeminfo|hostname' } |
          Select-Object TimeCreated, @{n='Cmd';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: |-
        No registry artifact from the commands.

        Commonly queried keys during system discovery:
          HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion (OS version)
          HKLM\SYSTEM\CurrentControlSet\Control\ComputerName (hostname)
    ossdetect: |-
        Sigma:
        - win_proc_creation_systeminfo.yml
        Elastic:
        - System Information Discovery
    tools: |-
        Sysmon (EID 1)
        Security EID 4688
        Auditd (Linux)
    actors:
        - APT41
        - APT28
    analyst_notes: systeminfo is notable because it returns the hotfix list, which attackers cross-reference against known LPE CVEs. An attacker running systeminfo followed by searching hotfixes for missing patches is a distinct chain. On Linux, uname -a + cat /etc/os-release + dpkg -l is the equivalent sequence.
