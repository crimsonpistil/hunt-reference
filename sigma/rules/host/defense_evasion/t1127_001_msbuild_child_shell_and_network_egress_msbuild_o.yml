# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/defense_evasion.js
title: T1127.001 - MSBuild Child Shell and Network Egress (MSBuild outbound network)
id: ce25e280-c229-5b7e-922a-587af7a1d19b
status: experimental
description: 'MSBuild.exe spawning a shell or making outbound network connections after compiling an inline task, or being invoked by a non-developer parent process (Office, script host, explorer), indicating abuse of the trusted build tool for fileless code execution Query block: MSBuild outbound network.'
references:
    - MITRE ATT&CK T1127.001
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1127.001
    - attack.g0016
    - attack.g0096
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name: MSBuild.exe
        event.category: network
    filter_0:
        destination.ip:
            - 10.*
            - 172.16.*
            - 192.168.*
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/defense_evasion.js
    domain: host
    technique: T1127.001
    technique_name: 'Trusted Developer Utilities: MSBuild'
    os: win
    query_block: 3 of 3
    original_query: |-
        process.name: "MSBuild.exe"
        AND event.category: "network"
        AND NOT destination.ip: (10.* OR 172.16.* OR 192.168.*)
    sysmon: |-
        // Sysmon EID 1 - MSBuild spawning shells or network tools
        ParentImage=*\MSBuild.exe
        Image=(*\powershell.exe OR *\cmd.exe OR *\rundll32.exe)

        // Sysmon EID 1 - MSBuild invoked by non-dev parent
        Image=*\MSBuild.exe
        ParentImage=(*\winword.exe OR *\excel.exe OR *\outlook.exe
          OR *\powershell.exe OR *\cmd.exe OR *\explorer.exe
          OR *\wscript.exe OR *\mshta.exe)

        // Sysmon EID 3 - MSBuild outbound network
        Image=*\MSBuild.exe
        DestinationPort=(80 OR 443 OR 4444 OR 8080 OR 8443)
    powershell: |-
        # Hunt for MSBuild suspicious parents
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -ErrorAction SilentlyContinue |
          Where-Object { $_.Properties[4].Value -like '*MSBuild*' } |
          Select-Object TimeCreated,
            @{n='Parent';e={$_.Properties[20].Value}},
            @{n='CmdLine';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: No persistent registry artifact from the proxy execution itself.
    ossdetect: |-
        Sigma:
        - proc_creation_win_msbuild_spawn_shell.yml
        - proc_creation_win_msbuild_susp_parent.yml
        - net_connection_win_msbuild_network.yml

        Elastic Detection Rules:
        - MSBuild Making Network Connections
        - Microsoft Build Engine Started by Suspicious Parent
    tools: |-
        Sysmon (EID 1 parent-child + EID 3 network)
        EDR behavioral detection
        .NET ETW tracing (Assembly.Load events)
    actors:
        - APT29
        - APT41
    analyst_notes: 'MSBuild abuse is distinctive because legitimate MSBuild is invoked by CI/CD pipelines, Visual Studio, and developer scripts, never by Office applications or user shells. The parent-process check (Office/script host parent + MSBuild child) is the highest-confidence single detection. Network egress adds a second behavioral layer: legitimate MSBuild fetches NuGet packages from known registries, not from arbitrary external IPs.'
