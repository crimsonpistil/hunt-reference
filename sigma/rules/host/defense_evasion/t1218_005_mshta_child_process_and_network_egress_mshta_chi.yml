# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/defense_evasion.js
title: T1218.005 - Mshta Child Process and Network Egress (mshta child shell processes)
id: b0a3d642-3afa-5328-ae1e-0f9b22044185
status: experimental
description: 'mshta.exe spawning a shell process (powershell, cmd, wscript, cscript) or making outbound HTTP/HTTPS connections to fetch remote HTA payloads, both strong behavioral indicators of maldoc-chain or inline-script abuse Query block: mshta child shell processes.'
references:
    - MITRE ATT&CK T1218.005
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1218.005
    - attack.g0096
    - attack.g0032
    - attack.g0094
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.parent.name: mshta.exe
        process.name:
            - powershell.exe
            - cmd.exe
            - wscript.exe
            - cscript.exe
            - rundll32.exe
    condition: selection
falsepositives:
    - The parent-child edge mshta->powershell or mshta->cmd is high-confidence malicious with near-zero false positives in enterprise environments
level: high
tonk:
    fidelity: full
    source_file: host/js/data/defense_evasion.js
    domain: host
    technique: T1218.005
    technique_name: 'System Binary Proxy Execution: Mshta'
    os: win
    query_block: 1 of 2
    original_query: |-
        process.parent.name: "mshta.exe"
        AND process.name: ("powershell.exe" OR "cmd.exe" OR "wscript.exe"
          OR "cscript.exe" OR "rundll32.exe")
    sysmon: |-
        // Sysmon EID 1 - mshta spawning a shell (the second-stage tell)
        ParentImage=*\mshta.exe
        Image=(*\powershell.exe OR *\cmd.exe OR *\wscript.exe
          OR *\cscript.exe OR *\rundll32.exe OR *\regsvr32.exe)

        // Sysmon EID 3 - mshta outbound network (remote HTA fetch)
        Image=*\mshta.exe
        DestinationPort=(80 OR 443 OR 8080 OR 8443)

        // Sysmon EID 22 - mshta DNS resolution
        Image=*\mshta.exe
        QueryName != *.microsoft.com
    powershell: |-
        # Hunt for mshta child processes (Sysmon EID 1)
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 10000 -ErrorAction SilentlyContinue |
          Where-Object { $_.Properties[20].Value -like '*mshta*' } |
          Select-Object TimeCreated,
            @{n='Child';e={$_.Properties[4].Value}},
            @{n='CmdLine';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: |-
        No persistent registry artifact from child spawn itself.
        mshta-spawned scripts may write Run keys or scheduled tasks
        as follow-on persistence.
    ossdetect: |-
        Sigma:
        - proc_creation_win_mshta_spawn_shell.yml
        - net_connection_win_mshta_network_activity.yml

        Elastic Detection Rules:
        - Mshta Making Network Connections
        - Suspicious Mshta Child Process
    tools: |-
        Sysmon (EID 1 parent-child + EID 3 network)
        EDR parent-child telemetry
        Proxy logs (mshta User-Agent fetching remote .hta)
    actors:
        - APT41
        - Lazarus
        - Kimsuky
    analyst_notes: 'mshta child-process detection catches the second stage: after the HTA/inline script executes, it almost always spawns a shell or LOLBin for the next phase. The parent-child edge mshta->powershell or mshta->cmd is high-confidence malicious with near-zero false positives in enterprise environments. Combining the network egress (remote HTA fetch) with the child spawn (script execution) gives a two-event chain that is essentially proof of the full attack sequence.'
