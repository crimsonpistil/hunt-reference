# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: host/js/data/defense_evasion.js
title: T1218.004 - InstallUtil Child Shell and Network Egress (InstallUtil outbound network)
id: 77090827-7495-5111-a858-108d5e4bcda1
status: experimental
description: 'InstallUtil.exe spawning a shell, making outbound network connections, or loading assemblies that perform runtime compilation (csc.exe/Roslyn), indicating the /U uninstall method is executing attacker code under a trusted .NET binary Query block: InstallUtil outbound network.'
references:
    - MITRE ATT&CK T1218.004
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1218.004
    - attack.g0096
    - attack.g0032
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        process.name: InstallUtil.exe
        event.category: network
    filter_0:
        destination.ip:
            - 10.*
            - 172.16.*
            - 192.168.*
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: host/js/data/defense_evasion.js
    domain: host
    technique: T1218.004
    technique_name: 'System Binary Proxy Execution: InstallUtil'
    os: win
    query_block: 2 of 2
    original_query: |-
        process.name: "InstallUtil.exe"
        AND event.category: "network"
        AND NOT destination.ip: (10.* OR 172.16.* OR 192.168.*)
    sysmon: |-
        // Sysmon EID 1 - InstallUtil child processes
        ParentImage=*\InstallUtil.exe
        Image=(*\powershell.exe OR *\cmd.exe OR *\rundll32.exe
          OR *\csc.exe OR *\vbc.exe)

        // Sysmon EID 3 - InstallUtil network egress
        Image=*\InstallUtil.exe
        DestinationPort=(80 OR 443 OR 4444 OR 8080 OR 8443)

        // Sysmon EID 7 - suspicious image loads by InstallUtil
        Image=*\InstallUtil.exe
        ImageLoaded=(*\System.Net.* OR *\System.Reflection.*)
    powershell: |-
        # Hunt for InstallUtil spawning children or network
        Get-WinEvent -FilterHashtable @{
          LogName='Microsoft-Windows-Sysmon/Operational'; Id=1
        } -MaxEvents 5000 -ErrorAction SilentlyContinue |
          Where-Object { $_.Properties[20].Value -like '*InstallUtil*' } |
          Select-Object TimeCreated,
            @{n='Child';e={$_.Properties[4].Value}},
            @{n='CmdLine';e={$_.Properties[10].Value.Substring(0,200)}}
    registry: No persistent registry artifact from the proxy execution itself.
    ossdetect: |-
        Sigma:
        - proc_creation_win_installutil_child_process.yml
        - net_connection_win_installutil_network.yml

        Elastic Detection Rules:
        - InstallUtil Network Activity
    tools: |-
        Sysmon (EID 1 child + EID 3 network + EID 7 image loads)
        EDR behavioral detection
        Application control (WDAC/AppLocker managed installer rules)
    actors:
        - APT41
        - Lazarus
    analyst_notes: InstallUtil proxy execution is quiet on the command line (just a path to a .dll or .exe with /U), so command-line detection alone is brittle. The behavioral signals (child processes, network egress, runtime compilation) are more reliable because the loaded assembly's actual behavior is what matters, not the InstallUtil invocation itself.
