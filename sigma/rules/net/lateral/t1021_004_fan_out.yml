# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/lateral.js
title: T1021.004 - Fan-out
id: 3dcf6f83-85a0-5205-9712-f19fcc61d00a
status: experimental
description: SSH fan-out from single source - one host SSH'ing to many destinations
references:
    - MITRE ATT&CK T1021.004
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.lateral-movement
    - attack.t1021.004
    - attack.g0096
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port: 22
        network.protocol: ssh
    filter_0:
        source.ip: $AUTOMATION_HOSTS
    condition: selection and not filter_0
falsepositives:
    - The detection has minimal false positives in environments where SSH automation is centralized to specific source hosts
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/lateral.js
    domain: net
    technique: T1021.004
    technique_name: 'Remote Services: SSH'
    original_query: |-
        source.ip: $MPNET
        AND NOT source.ip: $AUTOMATION_HOSTS
        AND destination.port: 22
        AND network.protocol: ssh
    arkime: |-
        ip.src == $MPNET
        && ip.src != $AUTOMATION_HOSTS
        && port.dst == 22
        && protocols == ssh
    suricata: |-
        alert tcp $HOME_NET any
          -> $HOME_NET 22
          (msg:"TA0008 T1021.004 SSH fan-out
            single source many destinations";
          flow:established,to_server;
          content:"SSH-"; depth:4;
          threshold:type both,
            track by_src,
            count 5, seconds 600;
          classtype:trojan-activity;
          sid:9100402; rev:1;)
    requires_variables:
        - $AUTOMATION_HOSTS
        - $MPNET
    actors:
        - APT41
        - Lazarus
        - Multi
    analyst_notes: 'Adversaries with valid SSH credentials/keys often work through Linux infrastructure methodically. The pattern: one source SSHing to 5+ distinct destinations within 10 minutes. Legitimate automation (Ansible runs, configuration management) produces similar patterns from $AUTOMATION_HOSTS - exclude these. After exclusions, sustained SSH fan-out is essentially always either red team or threat actor. The detection has minimal false positives in environments where SSH automation is centralized to specific source hosts. Build $AUTOMATION_HOSTS to include your actual sanctioned Ansible/Salt/Puppet/Chef sources. Cloud environments may need additional exclusions for CI/CD systems and orchestration tools.'
