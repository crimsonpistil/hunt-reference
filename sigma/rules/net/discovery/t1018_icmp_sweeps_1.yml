# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1018 - ICMP Sweeps
id: 22a18a7e-a5c2-51ac-988a-4bff44bbb209
status: experimental
description: ICMP fan-out from non-monitoring host - sustained ping pattern across multiple subnets
references:
    - MITRE ATT&CK T1018
    - industry threat hunting guidance
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1018
    - attack.g0096
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        network.protocol: icmp
        icmp.type: 8
    filter_0:
        source.ip: $MONITORING_HOSTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1018
    technique_name: Remote System Discovery
    original_query: |-
        source.ip: $MPNET
        AND NOT source.ip: $MONITORING_HOSTS
        AND network.protocol: icmp
        AND icmp.type: 8
    arkime: |-
        ip.src == $MPNET
        && ip.src != $MONITORING_HOSTS
        && protocols == icmp
        && icmp.type == 8
    suricata: |-
        alert icmp $HOME_NET any
          -> $HOME_NET any
          (msg:"TA0007 T1018 ICMP fan-out
            across multiple subnets
            network reconnaissance";
          itype:8;
          threshold:type both,
            track by_src,
            count 100, seconds 300;
          classtype:attempted-recon;
          sid:9101802; rev:1;)
    requires_variables:
        - $MONITORING_HOSTS
        - $MPNET
    actors:
        - APT41
        - Lazarus
        - Ransomware
        - Multi
    analyst_notes: 'More refined than sid 9101801: looks for ping activity spanning multiple subnets from one source within 5 minutes. Adversary doing network mapping pings 10.0.1.0/24, then 10.0.2.0/24, then 10.0.3.0/24 - covering the address space methodically. This pattern is much rarer than single-subnet pings (which can be legitimate broadcast-storm-investigation). Multi-subnet ICMP fan-out from a workstation is essentially always reconnaissance. Tune by counting distinct /24s touched: 3+ subnets = strong signal, 5+ = certain reconnaissance. Combine with packet timing analysis - adversary scanners often produce highly regular timing (every 10ms exactly), legitimate troubleshooting is bursty.'
