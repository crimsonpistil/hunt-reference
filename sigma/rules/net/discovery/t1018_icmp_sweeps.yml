# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1018 - ICMP Sweeps
id: 22a18a7e-a5c2-51ac-988a-4bff44bbb209
status: experimental
description: ICMP echo sweep - single source pinging many internal hosts in a short window
references:
    - MITRE ATT&CK T1018
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1018
    - attack.g0096
    - attack.g0016
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.ip: $MPNET
        network.protocol: icmp
        icmp.type: 8
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: low
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1018
    technique_name: Remote System Discovery
    original_query: |-
        source.ip: $MPNET
        AND destination.ip: $MPNET
        AND network.protocol: icmp
        AND icmp.type: 8
    arkime: |-
        ip.src == $MPNET
        && protocols == icmp
        && icmp.type == 8
        && ip.dst == $MPNET
    suricata: |-
        alert icmp $HOME_NET any
          -> $HOME_NET any
          (msg:"TA0007 T1018 ICMP echo
            sweep many internal hosts
            discovery";
          itype:8;
          threshold:type both,
            track by_src,
            count 20, seconds 60;
          classtype:attempted-recon;
          sid:9101801; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - APT29
        - Ransomware
        - Multi
    analyst_notes: 'The classic host-discovery primitive: ping every IP in a subnet, see who responds. nmap -sn, fping, PowerShell Test-Connection loops, custom scanners all produce this pattern. Suricata''s `track by_src, count 20, seconds 60` aggregates by source - alerting only when one host pings 20+ distinct destinations in a minute. Tune the threshold to your environment: monitoring tools (Smokeping, sanctioned scanners) may legitimately ping many hosts and should be in $MONITORING_HOSTS exclusion. Workstations and servers shouldn''t initiate ICMP sweeps. The detection is noisy in environments with active monitoring infrastructure - invest the time to build a clean exclusion list. Pair with EDR for definitive process attribution (ping.exe, nmap.exe, PowerShell, Python).'
