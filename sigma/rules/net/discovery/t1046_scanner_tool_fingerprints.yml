# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1046 - Scanner Tool Fingerprints
id: 02c91672-f138-5eee-9efc-28b620a6eace
status: experimental
description: Masscan / zmap fingerprint - extremely high-rate SYN scan with characteristic packet structure
references:
    - MITRE ATT&CK T1046
    - industry tooling
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1046
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        tcp.flags: S
        tcp.window:
            - 1024
            - 0
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1046
    technique_name: Network Service Discovery
    original_query: |-
        source.ip: $MPNET
        AND tcp.flags: "S"
        AND tcp.window: (1024 OR 0)
    suricata: |-
        alert tcp $HOME_NET any
          -> $HOME_NET any
          (msg:"TA0007 T1046 Masscan or
            zmap high-rate SYN scan
            fingerprint";
          flow:stateless;
          flags:S;
          window:1024;
          threshold:type both,
            track by_src,
            count 500, seconds 10;
          classtype:attempted-recon;
          sid:9104607; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Ransomware
        - Multi
    analyst_notes: 'masscan and zmap are mass-scanning tools designed for internet-wide scans - they produce SYN packets at rates 100-1000x higher than nmap (millions of packets per second possible). They use distinctive packet structures: small TCP window sizes (1024 or 0), specific TTL values, and hardcoded sequence number patterns. Detection: a single source generating 500+ SYN packets in 10 seconds is essentially never legitimate traffic - even aggressive vulnerability scanners run slower than this. Internal masscan use is rare but appears in some red team and ransomware affiliate operations for rapid pre-encryption network mapping. Tune the threshold based on your scanner allowlist - if Nessus runs at moderate rates, 500/10s catches masscan but not Nessus.'
