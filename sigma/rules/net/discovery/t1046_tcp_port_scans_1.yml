# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1046 - TCP Port Scans
id: 87cc5129-6809-5e2d-bc16-3ede6f03202c
status: experimental
description: Horizontal port sweep - single port across many hosts (service discovery)
references:
    - MITRE ATT&CK T1046
    - CISA AA23-320A
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1046
    - attack.g1015
    - attack.g0016
    - attack.g0096
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port:
            - 22
            - 445
            - 3389
            - 5985
            - 5986
            - 1433
            - 3306
            - 5432
            - 6379
            - 27017
    filter_0:
        source.ip: $SCAN_SOURCES
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1046
    technique_name: Network Service Discovery
    original_query: |-
        source.ip: $MPNET
        AND NOT source.ip: $SCAN_SOURCES
        AND destination.port: (
          22 OR 445 OR 3389 OR 5985
          OR 5986 OR 1433 OR 3306
          OR 5432 OR 6379 OR 27017
        )
    arkime: |-
        ip.src == $MPNET
        && ip.src != $SCAN_SOURCES
        && protocols == tcp
        && port.dst == [22, 445, 3389, 5985, 5986, 1433, 3306, 5432, 6379, 27017]
    suricata: |-
        alert tcp $HOME_NET any
          -> $HOME_NET [22,445,3389,
           5985,5986,1433,3306,5432,
           6379,27017]
          (msg:"TA0007 T1046 Horizontal
            port sweep specific service
            across many hosts";
          flow:to_server;
          flags:S;
          threshold:type both,
            track by_src,
            count 20, seconds 60;
          classtype:attempted-recon;
          sid:9104602; rev:1;)
    requires_variables:
        - $MPNET
        - $SCAN_SOURCES
    actors:
        - Scattered Spider
        - APT29
        - APT41
        - Ransomware
        - Multi
    analyst_notes: 'Horizontal scan = one port, many hosts. The adversary wants to find every host running a specific service: every SSH server (22), every SMB share (445), every RDP host (3389), every WinRM endpoint (5985/5986), every SQL server (1433/3306/5432), every Redis (6379), every MongoDB (27017). This is the most common scan pattern in lateral movement preparation - find the target service, then attempt credential abuse against it. The signal is much cleaner than vertical scans because legitimate clients don''t connect to one service across 20+ hosts in a minute (DCs and management hosts are rare exceptions). Build $SERVICE_CLIENTS allowlist for legitimate per-service-port traffic patterns. Workstations sweeping RDP across many hosts = essentially always pre-lateral-movement reconnaissance.'
