# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1482 - Cross-Trust DNS Discovery
id: 76207862-86a5-5930-ae3f-ed2e51b5ddee
status: experimental
description: Cross-trust DNS query - SRV record lookup for foreign domain DC discovery
references:
    - MITRE ATT&CK T1482
    - T1558.001
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1482
    - attack.g0016
    - attack.g0096
logsource:
    category: dns
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        dns.question.type: SRV
        dns.question.name: '*_ldap._tcp.*'
    filter_0:
        dns.question.name: '*.$LOCAL_DOMAIN'
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1482
    technique_name: Domain Trust Discovery
    original_query: |-
        source.ip: $MPNET
        AND dns.question.type: "SRV"
        AND dns.question.name: *_ldap._tcp.*
        AND NOT dns.question.name: *.$LOCAL_DOMAIN
    arkime: |-
        ip.src == $MPNET
        && protocols == dns
        && dns.query.type == SRV
        && host.dns == "_ldap._tcp.*"
        && host.dns != $LOCAL_DOMAIN_SRV
    suricata: |-
        alert dns $HOME_NET any
          -> any 53
          (msg:"TA0007 T1482 Cross-trust
            SRV query foreign domain DC
            discovery";
          flow:stateless;
          dns.query;
          pcre:"/_ldap\._tcp\./i";
          threshold:type both,
            track by_src,
            count 5, seconds 600;
          classtype:attempted-recon;
          sid:9148205; rev:1;)
    requires_variables:
        - $LOCAL_DOMAIN
        - $MPNET
    actors:
        - APT29
        - APT41
        - Ransomware
        - Multi
    analyst_notes: 'After enumerating trust relationships, adversaries discover the DCs of trusted domains via DNS SRV record lookups: _ldap._tcp.<trustdomain.local>, _kerberos._tcp.<trustdomain.local>, _ldap._tcp.dc._msdcs.<trustdomain.local>. These queries reveal the foreign DC IPs needed for cross-trust attacks (SID History abuse, foreign Kerberos ticket forging). Detection: SRV queries for _ldap._tcp or _kerberos._tcp outside your local domain namespace. Build $LOCAL_DOMAIN_SRV from your AD topology - anything else querying foreign domain SRV records is anomalous. Particularly relevant in enterprises with M&A-acquired domains or partner forest trusts. Pair with subsequent connections to the foreign DC IPs for confirmation of cross-trust progression.'
