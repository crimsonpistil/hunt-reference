# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1046 - TCP Port Scans
id: 87cc5129-6809-5e2d-bc16-3ede6f03202c
status: experimental
description: TCP SYN scan - half-open probes to many ports on single target
references:
    - MITRE ATT&CK T1046
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1046
    - attack.g0096
    - attack.g0032
    - attack.g0016
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        tcp.flags: S
    filter_0:
        source.ip: $SCAN_SOURCES
    filter_1:
        tcp.flags: A
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - legitimate vulnerability scanners (Nessus, Qualys, Rapid7, OpenVAS) produce identical patterns and MUST be in $SCAN_SOURCES exclusion
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1046
    technique_name: Network Service Discovery
    original_query: |-
        source.ip: $MPNET
        AND NOT source.ip: $SCAN_SOURCES
        AND tcp.flags: "S"
        AND NOT tcp.flags: "A"
    suricata: |-
        alert tcp $HOME_NET any
          -> $HOME_NET any
          (msg:"TA0007 T1046 TCP SYN scan
            many ports single target";
          flags:S,12;
          threshold:type both,
            track by_src,
            count 30, seconds 60;
          classtype:attempted-recon;
          sid:9104601; rev:1;)
    requires_variables:
        - $MPNET
        - $SCAN_SOURCES
    actors:
        - APT41
        - Lazarus
        - APT29
        - Ransomware
        - Multi
    analyst_notes: 'The most fundamental port scan: send SYN packets to many destination ports on a single target. Open ports respond with SYN-ACK, closed ports with RST, filtered ports drop silently. nmap -sS (default scan), masscan, zmap, custom scanners all produce this pattern. Detection: count distinct destination ports per (src, dst) pair - 30+ in 60s is essentially certain scanning behavior. False positives: legitimate vulnerability scanners (Nessus, Qualys, Rapid7, OpenVAS) produce identical patterns and MUST be in $SCAN_SOURCES exclusion. Build the exclusion carefully - these scanners often run from dedicated VLANs or specific source IPs. After exclusions, this detection is near-zero-FP. Pair with EDR for nmap.exe, masscan, PowerShell Test-NetConnection loops attribution.'
