# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/discovery.js
title: T1482 - LDAP Trust Enumeration
id: 21a5c9b2-6383-5c03-b806-0fc03afe11ac
status: experimental
description: LDAP query for trustedDomain objects - direct trust relationship enumeration
references:
    - MITRE ATT&CK T1482
    - BloodHound documentation
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.discovery
    - attack.t1482
    - attack.g0016
    - attack.g0096
    - attack.g1015
logsource:
    category: network_connection
    product: zeek
    service: ldap
detection:
    selection:
        source.ip: $MPNET
        destination.port:
            - 389
            - 636
        ldap.filter:
            - '*objectClass=trustedDomain*'
            - '*objectCategory=trustedDomain*'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/discovery.js
    domain: net
    technique: T1482
    technique_name: Domain Trust Discovery
    original_query: |-
        source.ip: $MPNET
        AND destination.port: (389 OR 636)
        AND ldap.filter: (
          *objectClass=trustedDomain*
          OR *objectCategory=trustedDomain*
        )
    suricata: |-
        alert tcp $HOME_NET any
          -> $DOMAIN_CONTROLLERS
          [389,636]
          (msg:"TA0007 T1482 LDAP query
            for trustedDomain objects
            trust enumeration";
          flow:established,to_server;
          content:"trustedDomain";
          classtype:attempted-recon;
          sid:9148201; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT29
        - APT41
        - Scattered Spider
        - Ransomware
        - Multi
    analyst_notes: 'trustedDomain objects in AD''s CN=System container store the configuration of every trust relationship: trust direction (inbound/outbound/bidirectional), trust type (parent-child, external, forest), trust transitivity, and SID filtering settings. Querying for objectClass=trustedDomain returns the full list of trusts. Tools: PowerView Get-DomainTrust, ldapsearch, BloodHound (collects trust data automatically), Get-ADTrust (RSAT). Legitimate AD-aware applications occasionally query trust info - but bulk enumeration from a workstation source is anomalous. The query is essentially a flag for upcoming cross-trust attack tooling. Pair with subsequent Kerberos cross-realm ticket requests (TGT requests against trusted domain DCs) for confirmation of attack progression.'
