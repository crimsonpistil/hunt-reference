# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1095 - L3/L4 Protocol Abuse
id: 1e645c51-a9b2-5a70-8f94-46a2293c67a5
status: experimental
description: Anomalous TCP flag combinations - covert channels in TCP header fields
references:
    - MITRE ATT&CK T1095
    - academic research
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1095
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        tcp.flags:
            - FIN+URG+PSH
            - 'NULL'
            - FIN
    condition: selection
falsepositives:
    - stack fingerprinting tools (nmap, masscan with custom options) - these should originate from sanctioned scan sources only
level: high
tonk:
    fidelity: full
    source_file: net/js/data/c2.js
    domain: net
    technique: T1095
    technique_name: Non-Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND tcp.flags: (
          "FIN+URG+PSH"
          OR "NULL"
          OR "FIN"
        )
    suricata: |-
        alert tcp $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1095 Anomalous
            TCP flag combination scan
            or covert channel";
          flags:FPU,12;
          classtype:bad-unknown;
          sid:9109508; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Multi
    analyst_notes: 'TCP flag combinations like FIN+URG+PSH (Christmas tree scan), NULL (no flags set), or lone FIN are not used by legitimate TCP stacks - they''re either scan attempts or covert channel signaling. Some advanced implants use unusual flag combinations or sequence number patterns for low-bandwidth covert C2 (each connection attempt encodes 1-2 bits of data). The detection catches both reconnaissance scans and the rare custom implant that uses TCP header fields as a covert channel. False positives: stack fingerprinting tools (nmap, masscan with custom options) - these should originate from sanctioned scan sources only. Outbound from a workstation = essentially always anomalous.'
