# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1071.001 - Web Protocols
id: fa5a5a6e-acbb-55cf-8a2f-ac6779055b75
status: experimental
description: 'HTTP POST beacon - outbound POST with small body, characteristic of beacon check-in / task response NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1071.001
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1071
    - attack.t1071.001
    - attack.g0016
    - attack.g0045
    - attack.g0032
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: POST
        http.request.body.bytes|lt: 5000
        http.response.body.bytes|lt: 1000
        url.path:
            - '*submit.php*'
            - '*api/v1/upload*'
            - '*upload/file*'
            - '*push*'
            - '*report*'
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/c2.js
    domain: net
    technique: T1071
    technique_name: Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: POST
        AND http.request.body.bytes < 5000
        AND http.response.body.bytes < 1000
        AND url.path: (
          *submit.php* OR *api/v1/upload*
          OR *upload/file*
          OR *push* OR *report*
        )
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == POST
        && databytes.src < 5000
        && databytes.dst < 1000
        && http.uri == ["*/submit.php", "*/api/v1/upload", "*/upload/file", "*/push", "*/report"]
        && ip.dst != $ALLOWED_DEFAULTS
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1071.001 HTTP
            POST beacon small body
            periodic checkin";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(submit\.php|api\/v1\/
            upload|upload\/file|push|
            report)/i";
          http.uri;
          threshold:type both,
            track by_src,
            count 3, seconds 1800;
          classtype:trojan-activity;
          sid:9107102; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (http.request.body.bytes < 5000): emitted as |lt modifier, backend support varies'
        - 'numeric-comparison (http.response.body.bytes < 1000): emitted as |lt modifier, backend support varies'
    actors:
        - APT29
        - APT10
        - Lazarus
    analyst_notes: POST-based beacons send results back to C2 - typically small encrypted/encoded payloads (a few KB) at regular intervals. Cobalt Strike default profile uses /submit.php; Sliver uses configurable URIs (often /api/...); custom implants use generic-looking endpoints. POST size 1-5KB combined with periodic timing is characteristic. Distinguish from legitimate API traffic by domain reputation and JA3/JA4 fingerprint. Watch for paired GET (task fetch) and POST (result return) sequences from same source-destination pair within seconds of each other - classic beacon RPC pattern.
