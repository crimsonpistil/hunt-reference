# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1095 - ICMP Tunneling
id: d4ea9274-12cc-5b8f-9096-23b57c8b704b
status: experimental
description: 'ICMP echo with high-entropy payload - encrypted data in ping NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1095
    - RITA documentation
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1095
    - attack.g0034
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        network.protocol: icmp
        icmp.type: 8
        _exists_: payload.entropy
        payload.entropy|gte: 6
        payload.entropy|lte: 8
    condition: selection
falsepositives:
    - some monitoring tools use larger ICMP payloads for testing, but they typically use predictable patterns (entropy <4)
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/c2.js
    domain: net
    technique: T1095
    technique_name: Non-Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND network.protocol: icmp
        AND icmp.type: 8
        AND _exists_: payload.entropy
        AND payload.entropy: [6.5 TO 8.0]
    suricata: |-
        alert icmp $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1095 ICMP echo
            with high entropy payload
            encrypted tunnel";
          itype:8;
          dsize:>60;
          classtype:trojan-activity;
          sid:9109502; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'range (payload.entropy 6.5 TO 8.0): split into gte/lte modifiers'
    actors:
        - Sandworm
        - Multi
    analyst_notes: 'Standard ping payloads have low entropy (sequential bytes, ASCII strings, predictable patterns). Encrypted ICMP tunnel data has near-maximum entropy (>6.5 bits per byte for the data portion). Combine with sid 9109501 (large payload size) for stronger signal. The Zeek ICMP analyzer combined with an entropy script can produce this metric. Most implants don''t bother with custom encryption inside ICMP tunnels - XOR or RC4 is common - but the entropy is still elevated above legitimate ping patterns. False positives: some monitoring tools use larger ICMP payloads for testing, but they typically use predictable patterns (entropy <4).'
