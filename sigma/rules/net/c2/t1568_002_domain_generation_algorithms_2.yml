# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1568.002 - Domain Generation Algorithms
id: f813dcc8-6252-5739-a184-98afd97bd5e6
status: experimental
description: 'Sequential failed DNS lookups followed by single successful query - DGA round successful resolution NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1568.002
    - CISA ED-21-01
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1568
    - attack.t1568.002
    - attack.g0016
logsource:
    category: dns
    product: zeek
detection:
    selection:
        destination.ip: $MPNET
        network.protocol: dns
    condition: selection
falsepositives:
    - 'False positive reduction: exclude resolutions to known CDN ranges (CloudFlare, Akamai, AWS) which sometimes appear in DGA-style patterns due to load balancing'
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/c2.js
    domain: net
    technique: T1568
    technique_name: Dynamic Resolution
    original_query: |-
        destination.ip: $MPNET
        AND network.protocol: dns
        AND ((dns.response_code: "NXDOMAIN" AND _exists_: dns.question.name)
          OR (dns.response_code: "NOERROR" AND dns.question.name: /[a-z0-9]{10,}\..+/))
    suricata: |-
        alert dns any 53
          -> $HOME_NET any
          (msg:"TA0011 T1568.002 NXDOMAIN
            burst followed by successful
            high-entropy resolution DGA
            success";
          flow:stateless;
          dns.response;
          threshold:type both,
            track by_dst,
            count 10, seconds 60;
          classtype:trojan-activity;
          sid:9156803; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'unparsed: ((dns.response_code: "NXDOMAIN" AND _exists_: dns.question.name) OR (dns.response_code: "NOERROR" AND dns.question.name:'
    actors:
        - APT29
    analyst_notes: 'The DGA successful resolution pattern: a flurry of NXDOMAIN responses (the implant trying generated domains) followed by a single NOERROR response (the operator''s registered domain). The successful domain is the actual C2 endpoint. Capture this domain immediately - it''s an active C2 IOC for the campaign. Build SIEM correlation rules that join NXDOMAIN bursts with subsequent successful queries from the same source within 60 seconds. The successful resolution IP is also a high-value IOC for blocklist propagation. False positive reduction: exclude resolutions to known CDN ranges (CloudFlare, Akamai, AWS) which sometimes appear in DGA-style patterns due to load balancing.'
