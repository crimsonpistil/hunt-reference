# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1568.003 - DNS Calculation
id: 758a3414-6af0-511e-81df-d15d097c1d0c
status: experimental
description: Internal host querying legitimate service for IP/port calculation seed - DNS calculation precursor
references:
    - MITRE ATT&CK T1568.003
    - T1016
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1568
    - attack.t1568.003
    - attack.g0007
    - attack.g0032
    - attack.g0096
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - '*icanhazip.com*'
            - '*ifconfig.me*'
            - '*api.ipify.org*'
            - '*checkip.amazonaws.com*'
            - '*ipinfo.io*'
            - '*iplogger.org*'
            - '*worldtimeapi.org*'
            - '*time.is*'
    condition: selection
falsepositives:
    - Treat as priority-1 escalation; do not dismiss as false positive without thorough investigation
level: high
tonk:
    fidelity: full
    source_file: net/js/data/c2.js
    domain: net
    technique: T1568
    technique_name: Dynamic Resolution
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          *icanhazip.com*
          OR *ifconfig.me*
          OR *api.ipify.org*
          OR *checkip.amazonaws.com*
          OR *ipinfo.io*
          OR *iplogger.org*
          OR *worldtimeapi.org*
          OR *time.is*
        )
    arkime: |-
        ip.src == $MPNET
        && protocols == https
        && host.http == ["*icanhazip.com*", "*ifconfig.me*", "*api.ipify.org*", "*checkip.amazonaws.com*", "*ipinfo.io*", "*iplogger.org*", "*worldtimeapi.org*", "*time.is*"]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1568.003 IP/time
            lookup possible DNS calculation
            seed";
          flow:established,to_server;
          pcre:"/Host:\s*(icanhazip|
            ifconfig\.me|api\.ipify|
            checkip\.amazonaws|
            ipinfo\.io|iplogger\.org|
            worldtimeapi\.org|
            time\.is)/i";
          http.header;
          threshold:type both,
            track by_src,
            count 5, seconds 3600;
          classtype:trojan-activity;
          sid:9156807; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT28
        - Lazarus
        - APT41
        - Multi
    analyst_notes: 'For process attribution, pivot to Kibana with Sysmon Event 3 filtered by the destination IP/port from this session - read winlog.event_data.Image for the culprit process. [AIR-GAP TRIPWIRE] This indicator detects connections to off-MPNet infrastructure that should be unreachable from an air-gapped environment. Any hit indicates a likely air-gap violation: bridged USB tether, rogue cellular modem, vendor/maintenance laptop bridging networks, supply-chain implant calling home, or misconfigured perimeter device. Treat as priority-1 escalation; do not dismiss as false positive without thorough investigation. DNS Calculation derives the C2 endpoint from external data - most commonly the implant queries a legitimate IP-lookup service (icanhazip, ifconfig.me, ipify) or time service (worldtimeapi, time.is) and uses the response in an algorithm to compute the actual C2 IP/port/domain. The lookup itself is benign; the inference comes from process correlation (workstation querying ipify isn''t a typical user action) and behavioral context (followed by anomalous outbound connection to a calculated destination). Many implants also use these services just for self-IP discovery (more T1016 Discovery than C2). Combine with subsequent outbound connection analysis: lookup followed quickly by connection to previously-unseen IP = high suspicion.'
