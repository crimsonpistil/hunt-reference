# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1071.001 - Web Protocols
id: fa5a5a6e-acbb-55cf-8a2f-ac6779055b75
status: experimental
description: 'HTTP beacon - periodic GET to same URI with low jitter, characteristic of Cobalt Strike, Sliver, Mythic, Havoc NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1071.001
    - S0154 Cobalt Strike
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1071
    - attack.t1071.001
    - attack.g0016
    - attack.g0096
    - attack.g0032
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: GET
        url.path:
            - '*/ca'
            - '*/dpixel'
            - '*/fwlink'
            - '*/pixel'
            - '*/__utm.gif'
            - '*/jquery-3.3.1.min.js'
            - '*/load'
            - '*/api/x'
        network.packets|lt: 50
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/c2.js
    domain: net
    technique: T1071
    technique_name: Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: GET
        AND url.path: (
          */ca OR */dpixel
          OR */fwlink OR */pixel
          OR */__utm.gif
          OR */jquery-3.3.1.min.js
          OR */load OR */api/x
        )
        AND network.packets < 50
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == GET
        && http.uri == ["*/ca", "*/dpixel", "*/fwlink", "*/pixel", "*/__utm.gif", "*/jquery-3.3.1.min.js", "*/load", "*/api/x"]
        && session.length > 60
        && packets.src < 50
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1071.001 HTTP
            beacon Cobalt Strike default
            profile URI";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/^\/(ca|dpixel|fwlink|
            pixel|__utm\.gif|
            jquery-3\.3\.1\.min\.js|
            load|api\/x)(\?|$)/i";
          http.uri;
          threshold:type both,
            track by_src,
            count 5, seconds 3600;
          classtype:trojan-activity;
          sid:9107101; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (network.packets < 50): emitted as |lt modifier, backend support varies'
    actors:
        - APT29
        - APT41
        - Lazarus
        - Multi
    analyst_notes: 'Default Cobalt Strike malleable profiles use predictable URIs - /ca, /dpixel, /fwlink, /pixel, /__utm.gif (mimics Google Analytics), /jquery-3.3.1.min.js (mimics CDN). Customized profiles change these but most operators don''t. Beacon periodicity is the more general signal: same internal-to-external pair with low jitter (sleep + small jitter window) over hours. Build per-pair session timing histograms in Kibana - periodic patterns stick out clearly. Pair with low databytes.src (beacon check-in is usually a few hundred bytes max) and low databytes.dst (no commands queued = small response).'
