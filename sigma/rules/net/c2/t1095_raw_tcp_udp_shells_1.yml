# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1095 - Raw TCP / UDP Shells
id: eb917929-ed31-5f91-b9df-2cafc029694c
status: experimental
description: 'Raw TCP on common web ports without HTTP/TLS - protocol mismatch indicating bind shell NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1095
    - T1571
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1095
    - attack.g0096
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port:
            - 80
            - 443
            - 8080
            - 8443
        event.duration|gt: 60000000
    filter_0:
        network.protocol:
            - http
            - tls
    filter_1:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - some custom internal applications use raw TCP on web ports (this should be on internal IPs only); some IoT devices use unusual protocols on standard ports
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/c2.js
    domain: net
    technique: T1095
    technique_name: Non-Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND destination.port: (
          80 OR 443 OR 8080 OR 8443
        )
        AND NOT network.protocol: (http OR tls)
        AND event.duration > 60000000
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && port.dst == [80, 443, 8080, 8443]
        && protocols != [http, tls]
        && session.length > 60
        && packets.src > 10
        && ip.dst != $ALLOWED_DEFAULTS
    suricata: |-
        alert tcp $HOME_NET any
          -> $EXTERNAL_NET [80,443,8080,8443]
          (msg:"TA0011 T1095 Raw TCP
            on web port no HTTP/TLS
            possible bind shell";
          flow:established,to_server;
          app-layer-protocol:!http;
          app-layer-protocol:!tls;
          threshold:type both,
            track by_src,
            count 1, seconds 600;
          classtype:trojan-activity;
          sid:9109505; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (event.duration > 60000000): emitted as |gt modifier, backend support varies'
    actors:
        - APT41
        - Lazarus
        - Multi
    analyst_notes: 'Adversaries often use ports 80, 443, 8080, 8443 for raw TCP shells because these ports are universally allowed outbound - but the traffic is raw command/response, not HTTP or TLS. Zeek''s Dynamic Protocol Detection (DPD) identifies the actual protocol regardless of port. Raw TCP on a web port = strong indicator. Suricata''s app-layer-protocol negation works similarly. The detection catches bind shells, custom binary protocols, encrypted-but-not-TLS payloads, and raw netcat-style channels. False positives: some custom internal applications use raw TCP on web ports (this should be on internal IPs only); some IoT devices use unusual protocols on standard ports.'
