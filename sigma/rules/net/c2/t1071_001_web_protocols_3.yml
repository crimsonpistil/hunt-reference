# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1071.001 - Web Protocols
id: fa5a5a6e-acbb-55cf-8a2f-ac6779055b75
status: experimental
description: HTTP User-Agent anomaly - non-browser UA on browser-like traffic, missing UA, or known-malicious UA
references:
    - MITRE ATT&CK T1071.001
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1071
    - attack.t1071.001
    - attack.g0096
    - attack.g0007
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        user_agent.original:
            - '*python-requests*'
            - '*Go-http-client*'
            - '*curl/*'
            - '*Wget/*'
            - '*PowerShell*'
            - '*Microsoft BITS*'
            - '*WinHTTP*'
            - '*Mozilla/4.0*'
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - actual scripts, package managers (pip, npm), update agents - baseline these by pairing UA with destination domain (pip + pypi.org = legit; pip + unknown-domain = suspicious)
level: high
tonk:
    fidelity: full
    source_file: net/js/data/c2.js
    domain: net
    technique: T1071
    technique_name: Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND user_agent.original: (
          *python-requests*
          OR *Go-http-client*
          OR *curl/* OR *Wget/*
          OR *PowerShell*
          OR *Microsoft BITS*
          OR *WinHTTP*
          OR *Mozilla/4.0*
        )
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.user-agent == ["*python-requests*", "*Go-http-client*", "*curl/*", "*Wget/*", "*PowerShell*", "*Microsoft BITS*", "*WinHTTP*", "*Mozilla/4.0*"]
        && ip.dst != $ALLOWED_DEFAULTS
        && port.dst == [80, 443]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1071.001 Non
            browser User-Agent suspicious
            HTTP client";
          flow:established,to_server;
          content:"User-Agent|3a|";
          http.header;
          pcre:"/User-Agent:\s*
            (python-requests|
            Go-http-client|curl\/|
            Wget\/|PowerShell|
            Microsoft BITS|WinHTTP|
            Mozilla\/4\.0)/i";
          http.header;
          classtype:trojan-activity;
          sid:9107104; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - APT41
        - APT28
        - Lazarus
        - Multi
    analyst_notes: 'User-Agent strings reveal the HTTP library used - python-requests, Go-http-client, curl, wget, PowerShell, WinHTTP, BITS - which from end-user workstations browsing the web is anomalous. Mozilla/4.0 (no version after) is a hardcoded UA in many implants and old Cobalt Strike profiles. Empty or missing User-Agent on outbound HTTP is also suspicious - every legitimate browser sets one. Custom UAs containing only the OS name or a single word are common implant signatures. False positives: actual scripts, package managers (pip, npm), update agents - baseline these by pairing UA with destination domain (pip + pypi.org = legit; pip + unknown-domain = suspicious).'
