# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1071.001 - Web Protocols
id: fa5a5a6e-acbb-55cf-8a2f-ac6779055b75
status: experimental
description: 'HTTPS beacon - periodic TLS sessions to same destination with low jitter and small data volumes NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1071.001
    - MITRE D3FEND
    - industry research
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1071
    - attack.t1071.001
    - attack.g0016
    - attack.g0096
    - attack.g0032
    - attack.g0064
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port: 443
        network.transport: tcp
        network.packets|lt: 30
        network.bytes|lt: 10000
        event.duration|lt: 5000000
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - software update checks, telemetry, push notification keepalives - baseline these with allowlist and high-volume known endpoints first
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/c2.js
    domain: net
    technique: T1071
    technique_name: Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND destination.port: 443
        AND network.transport: tcp
        AND network.packets < 30
        AND network.bytes < 10000
        AND event.duration < 5000000
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == tls
        && port.dst == 443
        && packets.src < 30
        && packets.dst < 30
        && databytes.src < 10000
        && session.length < 5
    suricata: |-
        alert tls $HOME_NET any
          -> $EXTERNAL_NET 443
          (msg:"TA0011 T1071.001 HTTPS
            periodic short beacon possible
            C2 checkin";
          flow:established,to_server;
          content:"|16 03|"; depth:2;
          threshold:type both,
            track by_src,
            count 10, seconds 3600;
          classtype:trojan-activity;
          sid:9107103; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (network.packets < 30): emitted as |lt modifier, backend support varies'
        - 'numeric-comparison (network.bytes < 10000): emitted as |lt modifier, backend support varies'
        - 'numeric-comparison (event.duration < 5000000): emitted as |lt modifier, backend support varies'
    actors:
        - APT29
        - APT41
        - Lazarus
        - APT33
        - Multi
    analyst_notes: |-
        HTTPS beacons are the dominant modern C2 channel., but the signal isn't in the encrypted payload, it's in the connection metadata. Look for short sessions (under 5s), low packet count (under 30 each way), small data volume (under 10KB), regular timing to same destination. Build a beacon detection model on flow records: same src-dst pair, sessions within 10% of a fixed interval, low data volumes, sustained over 1+ hour. 

         Open-source: RITA (Real Intelligence Threat Analytics) and AC-Hunter implement this analysis on Zeek conn.log. False positives: software update checks, telemetry, push notification keepalives - baseline these with allowlist and high-volume known endpoints first.
