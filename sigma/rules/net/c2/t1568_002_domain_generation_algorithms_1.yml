# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1568.002 - Domain Generation Algorithms
id: f813dcc8-6252-5739-a184-98afd97bd5e6
status: experimental
description: High-entropy domain queries - algorithmically generated subdomain or domain string
references:
    - MITRE ATT&CK T1568.002
    - academic research
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1568
    - attack.t1568.002
    - attack.g0016
logsource:
    category: dns
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        dns.question.name|re: '[a-z0-9]{12,30}\.(com|net|org|info|biz|tk|ml|xyz|top)'
    filter_0:
        dns.question.name: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - hash-named CDN endpoints (CloudFront, Akamai), some shorteners (bit.ly looks DGA-like)
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/c2.js
    domain: net
    technique: T1568
    technique_name: Dynamic Resolution
    original_query: |-
        source.ip: $MPNET
        AND dns.question.name: /[a-z0-9]{12,30}\.(com|net|org|info|biz|tk|ml|xyz|top)/
        AND NOT dns.question.name: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == dns
        && host.dns != $ALLOWED_DEFAULTS
        // DGA / high-entropy detection requires regex - does not always play well in Arkime. 
        // See Suricata pcre column or use Kibana KQL regex syntax for runtime matching.

        // Logical spec: host.dns matches
        //   /^[a-z0-9]{12,30}\.(com|net|org|info|biz|us|ru|cn|tk|ml|ga|cf|xyz|top|online|site)$/
    suricata: |-
        alert dns $HOME_NET any
          -> any 53
          (msg:"TA0011 T1568.002 High
            entropy domain query possible
            DGA";
          flow:stateless;
          dns.query;
          pcre:"/^[a-z0-9]{12,30}\.
            (com|net|org|info|biz|us|
            ru|cn|tk|ml|ga|cf|xyz|top|
            online|site)$/i";
          classtype:trojan-activity;
          sid:9156802; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - APT29
    analyst_notes: 'DGA-generated domains have characteristic structure: 12-30 character base label with high character entropy (no dictionary words, near-uniform character distribution). Most DGAs target common TLDs (.com, .net, .org) with some variation in cheap TLDs. Calculate Shannon entropy on the second-level domain - DGA domains score >3.5; legitimate domains usually <3.0 due to dictionary words and brand patterns. Build word-list filters: domains where >30% of characters form recognizable English (or target-language) substrings are likely legitimate. False positives: hash-named CDN endpoints (CloudFront, Akamai), some shorteners (bit.ly looks DGA-like). Maintain an exclusion list of known high-entropy legitimate domains.'
