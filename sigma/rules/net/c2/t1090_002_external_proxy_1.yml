# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1090.002 - External Proxy
id: e50fc92d-fa7f-5137-9c99-d44e39d09493
status: experimental
description: HTTP CONNECT method outbound - open proxy abuse for tunneling
references:
    - MITRE ATT&CK T1090.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1090
    - attack.t1090.002
    - attack.g0096
    - attack.g0007
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: CONNECT
    filter_0:
        destination.ip: $MPNET_PROXIES
    filter_1:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/c2.js
    domain: net
    technique: T1090
    technique_name: Proxy
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: CONNECT
        AND NOT destination.ip: $MPNET_PROXIES
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == CONNECT
        && ip.dst != $MPNET_PROXIES
        && ip.dst != $ALLOWED_DEFAULTS
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1090.002 HTTP
            CONNECT method to external
            open proxy abuse";
          flow:established,to_server;
          content:"CONNECT "; depth:8;
          classtype:trojan-activity;
          sid:9109004; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
        - $MPNET_PROXIES
    actors:
        - APT41
        - APT28
        - Multi
    analyst_notes: 'HTTP CONNECT is used by web proxies to establish tunnels (typically HTTPS through a forward proxy). Legitimate use: clients configured with $MPNET_PROXIES tunneling out via your sanctioned forward proxy. Anomalous use: a workstation issuing CONNECT to an external IP that isn''t your proxy - indicates either misconfigured client or implant using an external open proxy as a relay. Open proxies (free, anonymous proxy lists) are heavily abused for adversary anonymization. The CONNECT method is rarely seen in normal HTTP traffic; when it appears outbound to non-corporate-proxy destinations it''s a strong signal. Zeek http.log captures the method explicitly.'
