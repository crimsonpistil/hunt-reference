# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/c2.js
title: T1095 - ICMP Tunneling
id: d4ea9274-12cc-5b8f-9096-23b57c8b704b
status: experimental
description: Sustained ICMP echo session - long-running ping pattern indicating active tunnel
references:
    - MITRE ATT&CK T1095
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.command-and-control
    - attack.t1095
    - attack.g0034
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        network.protocol: icmp
        icmp.type: 8
    filter_0:
        destination.ip:
            - $MPNET
            - $MONITORING_TARGETS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/c2.js
    domain: net
    technique: T1095
    technique_name: Non-Application Layer Protocol
    original_query: |-
        source.ip: $MPNET
        AND network.protocol: icmp
        AND icmp.type: 8
        AND NOT destination.ip: ($MPNET OR $MONITORING_TARGETS)
    arkime: |-
        ip.src == $MPNET
        && protocols == icmp
        && icmp.type == 8
    suricata: |-
        alert icmp $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0011 T1095 Sustained
            ICMP echo session possible
            active tunnel";
          itype:8;
          threshold:type both,
            track by_src,
            count 100, seconds 1800;
          classtype:trojan-activity;
          sid:9109503; rev:1;)
    requires_variables:
        - $MONITORING_TARGETS
        - $MPNET
    actors:
        - Sandworm
        - Multi
    analyst_notes: Active ICMP tunnels show as sustained ping patterns - hundreds of echo packets over half-hour windows. Legitimate ping use is bursty and short-lived (a few packets to test connectivity, then stops). Continuous ping flow over 30+ minutes from a workstation to an external destination is essentially never legitimate. Build a $MONITORING_TARGETS exclusion for sanctioned ping monitoring (Smokeping, internal monitoring sources pinging known-good targets). After exclusions, this is a near-zero-FP detection. Combine with payload analysis (sid 9109501, 9109502) for definitive identification.
