# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1133 - RDP Exposure
id: 7ea2ff8d-5f13-5fa5-949d-b70a98568d29
status: experimental
description: 'RDP credential spray - high-volume authentication attempts from single external source NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1133
    - T1110.003
    - FBI ransomware advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1133
    - attack.g0032
    - attack.g0007
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port: 3389
        network.transport: tcp
        network.packets|gt: 5
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1133
    technique_name: External Remote Services
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 3389
        AND network.transport: tcp
        AND network.packets > 5
    arkime: |-
        ip.src != $MPNET
        && port.dst == 3389
        && protocols == rdp
        && packets.src > 5
        && packets.dst > 5
        && databytes.dst < 5000
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET 3389
          (msg:"TA0001 T1133 RDP credential
            spray high volume auth
            attempts";
          flow:established,to_server;
          content:"|03 00|"; depth:2;
          threshold:type both,
            track by_src,
            count 10, seconds 60;
          classtype:attempted-user;
          sid:9113304; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (network.packets > 5): emitted as |gt modifier, backend support varies'
    actors:
        - Lazarus
        - APT28
    analyst_notes: 'RDP credential spraying tools (Hydra, Medusa, NLBrute, RDPBrute) generate high-volume authentication attempts visible as rapid successive TCP/3389 connections. Each attempt: TCP connect, TPKT/RDP handshake, NLA authentication exchange, disconnect - generating a distinctive connection pattern of many short sessions from the same source. Low databytes.dst confirms no successful session data was transferred. Distributed sprays use many source IPs - look for the same username being attempted across many sources.'
