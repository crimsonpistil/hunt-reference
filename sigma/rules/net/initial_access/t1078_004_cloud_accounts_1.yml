# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1078.004 - Cloud Accounts
id: 1456d6a9-3534-5d18-907f-ee6f3bf7714f
status: experimental
description: 'Federated identity token abuse - SAML golden ticket or OAuth token from anomalous issuer NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1078.004
    - T1606.002
    - CISA ED-21-01
    - Microsoft MSTIC
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1078
    - attack.t1078.004
    - attack.g0016
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        http.request.body: '*SAMLResponse=*'
        source.bytes|gt: 500
    filter_0:
        source.ip: $MPNET
    filter_1:
        url.domain: $ALLOWED_IDPS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1078
    technique_name: Valid Accounts
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: POST
        AND http.request.body:
          *SAMLResponse=*
        AND NOT url.domain: $ALLOWED_IDPS
        AND source.bytes > 500
    arkime: |-
        ip.src != $MPNET
        && protocols == https
        && http.method == POST
        && http.reqbody == ["*SAMLResponse=*", "*grant_type=urn:ietf:params:oauth:grant-type:saml2-bearer*"]
        && host.http != $ALLOWED_IDPS
        && databytes.src > 500
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $EXTERNAL_NET 443
          (msg:"TA0001 T1078.004 SAML
            response from unexpected
            issuer golden ticket abuse";
          flow:established,to_server;
          content:"POST"; http.method;
          content:"SAMLResponse=";
          http.request_body;
          classtype:policy-violation;
          sid:9107807; rev:1;)
    requires_variables:
        - $ALLOWED_IDPS
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 500): emitted as |gt modifier, backend support varies'
    actors:
        - APT29
        - Multi
    analyst_notes: 'SAML golden ticket attacks (Solorigate/SolarWinds TTPs) forge SAML tokens using a compromised ADFS token signing certificate - the forged token is accepted by cloud services (Azure AD, AWS) as legitimate. Network signal: SAMLResponse POST from an unexpected host or to a cloud service that should be receiving SAML assertions from your ADFS/IdP. Compare the SAMLResponse Issuer field against your known IdP entity IDs. Also watch for OAuth token grant requests using the SAML bearer assertion grant type from unexpected IPs.'
