# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1091 - Post-USB Execution
id: d050194e-3506-5042-8989-8998007e1679
status: experimental
description: 'New outbound beacon from host within 5 minutes of USB device insertion event NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1091
    - CISA advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1091
    - attack.g0034
    - attack.g0129
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port:
            - 443
            - 80
            - 8080
            - 53
            - 4444
        network.packets|gte: 5
        network.packets|lte: 50
        event.duration|gt: 60000000
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1091
    technique_name: Replication Through Removable Media
    original_query: |-
        source.ip: $MPNET
        AND NOT destination.ip: $ALLOWED_DEFAULTS
        AND destination.port: (
          443 OR 80 OR 8080
          OR 53 OR 4444
        )
        AND network.packets: [5 TO 50]
        AND event.duration > 60000000
    arkime: |-
        ip.src == $MPNET
        && ip.dst != $ALLOWED_DEFAULTS
        && port.dst == [443, 80, 8080, 53, 4444]
        && packets.src > 5
        && packets.src < 50
        && session.length > 60
        // USB event correlation is not a network signal -
        // requires SIEM join with EDR/Sysmon Event 9
        // (RawAccessRead on USB) or Windows Event 6416
        // (USB device insertion). The <300s post-insertion
        // window must be applied at SIEM correlation time.
    suricata: |-
        alert tcp $HOME_NET any
          -> $EXTERNAL_NET
          [80,443,8080,4444]
          (msg:"TA0001 T1091 New outbound
            beacon possible USB payload
            activation";
          flow:established,to_server;
          threshold:type both,
            track by_src,
            count 1, seconds 300;
          classtype:trojan-activity;
          sid:9109101; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    dropped_clauses:
        - 'range (network.packets 5 TO 50): split into gte/lte modifiers'
        - 'numeric-comparison (event.duration > 60000000): emitted as |gt modifier, backend support varies'
    actors:
        - Sandworm
        - Mustang Panda
        - Multi
    analyst_notes: 'Detection requires correlation between Windows Event 6416 (USB device insertion), Sysmon Event 9 (RawAccessRead on USB), or EDR USB telemetry and subsequent first-seen outbound network connections. The window is short - most USB-borne payloads beacon within seconds of execution. Build per-host network baselines: any new outbound destination IP appearing within 5 minutes of a USB event from a host that previously didn''t communicate with that destination is a strong signal. Air-gapped or OT networks: any outbound from a host that just had a USB inserted is itself anomalous regardless of destination.'
