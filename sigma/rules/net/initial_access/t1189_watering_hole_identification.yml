# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1189 - Watering Hole Identification
id: ab022cbe-08f4-532d-b18d-25d9182c146d
status: experimental
description: 'Internal user requesting known compromised / categorized malicious domain NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1189
    - T1584.004
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1189
    - attack.g0045
    - attack.g0007
    - attack.g0064
    - attack.g0032
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: GET
        destination.ip: $THREAT_INTEL_IPS
        source.bytes|gt: 0
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1189
    technique_name: Drive-by Compromise
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: GET
        AND destination.ip: $THREAT_INTEL_IPS
        AND source.bytes > 0
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && host.http == $EXTERNAL_TI_DOMAINS
        && http.method == GET
        && databytes.src > 0
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0001 T1189 Internal host
            connecting to known watering
            hole / malicious domain";
          flow:established,to_server;
          dns.query;
          content:"|00 01 00 00 00 00 00 00|";
          classtype:trojan-activity;
          sid:9118905; rev:1;)
    requires_variables:
        - $MPNET
        - $THREAT_INTEL_IPS
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - APT10
        - APT28
        - APT33
        - Lazarus
    analyst_notes: Threat intel feeds maintain lists of known watering hole domains and IPs - integrate these with Suricata's rule sets and Kibana's threat intel enrichment. For Arkime, maintain a $EXTERNAL_TI_DOMAINS field reference updated from your threat intel platform. A victim connecting to a known watering hole domain from inside your network = active drive-by in progress or already completed. Correlate with what was downloaded (databytes.dst) and any subsequent outbound connections from the same host - post-exploitation C2 often follows within seconds to minutes.
