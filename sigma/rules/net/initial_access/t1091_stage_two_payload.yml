# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1091 - Stage-Two Payload
id: e826e70c-0574-56d3-9f3f-a6589b5c9b2e
status: experimental
description: Internal host fetching second-stage payload after autorun-pattern execution - LNK / autorun.inf signal
references:
    - MITRE ATT&CK T1091
    - T1204.002
    - CISA advisories
    - Microsoft MSTIC
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1091
    - attack.g0129
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: GET
        url.path:
            - '*.bin'
            - '*.dat'
            - '*.exe'
            - '*.dll'
            - '*.ps1'
            - '*.scr'
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1091
    technique_name: Replication Through Removable Media
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: GET
        AND url.path: (
          *.bin OR *.dat
          OR *.exe OR *.dll
          OR *.ps1 OR *.scr
        )
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == GET
        && http.uri == ["*.bin", "*.dat", "*.exe", "*.dll", "*.ps1", "*.scr"]
        && ip.dst != $ALLOWED_DEFAULTS
        // Domain-age filtering and USB event correlation are
        // not available in baseline Arkime 4.3.1. Pair this
        // query with external domain-age enrichment for the
        // <30d signal, and join with EDR/Sysmon Event 9
        // (RawAccessRead on USB) or Windows Event 6416 in
        // the SIEM for the <60s post-USB-insertion correlation.
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0001 T1091 Second-stage
            payload fetch post USB possible
            autorun activation";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/\.(bin|dat|exe|dll|
            ps1|scr|ico)\b/i";
          http.uri;
          classtype:trojan-activity;
          sid:9109105; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - Raspberry Robin
        - Mustang Panda
        - Multi
    analyst_notes: 'Modern USB-borne malware often uses LNK files or registered file handlers (autorun.inf is largely deprecated but still abused on legacy systems) that execute a small first-stage downloader. The downloader fetches the actual payload from an external server. Network signal: HTTP GET for an executable, DLL, PowerShell script, or generic binary file from an unfamiliar host within 60 seconds of a USB device event. Raspberry Robin specifically uses .lnk files on USBs that fetch payloads from compromised QNAP devices. User-Agent often reveals the downloader: WinHTTP, BITS/7.5, PowerShell, certutil - these from a workstation right after USB insertion are highly suspicious.'
