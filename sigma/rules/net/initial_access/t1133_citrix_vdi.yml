# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1133 - Citrix / VDI
id: bb3b8138-28f2-566b-aaca-db59645d47a6
status: experimental
description: Citrix / RD Gateway authentication from unexpected source - cloud-hosted access broker abuse
references:
    - MITRE ATT&CK T1133
    - T1078
    - CISA advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1133
    - attack.g0096
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        url.domain:
            - '*citrix*'
            - '*netscaler*'
            - '*storefront*'
            - '*rdweb*'
            - '*horizon*'
            - '*workspaceone*'
        url.path:
            - '*cgi/login*'
            - '*vpn/index*'
            - '*LogonPoint*'
            - '*RDWeb*'
            - '*webclient*'
    filter_0:
        source.ip: $MPNET
    filter_1:
        source.ip: $ALLOWED_CITRIX_IPS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1133
    technique_name: External Remote Services
    original_query: |-
        NOT source.ip: $MPNET
        AND NOT source.ip: $ALLOWED_CITRIX_IPS
        AND http.request.method: POST
        AND url.domain: (
          *citrix* OR *netscaler*
          OR *storefront* OR *rdweb*
          OR *horizon* OR *workspaceone*
        )
        AND url.path: (
          *cgi/login* OR *vpn/index*
          OR *LogonPoint*
          OR *RDWeb* OR *webclient*
        )
    arkime: |-
        ip.src != $MPNET
        && ip.src != $ALLOWED_CITRIX_IPS
        && protocols == https
        && host.http == ["*citrix*", "*netscaler*", "*storefront*", "*rdweb*", "*rdgateway*", "*horizon*", "*workspaceone*"]
        && http.method == POST
        && http.uri == ["*/cgi/login*", "*/vpn/index*", "*/logon/LogonPoint*", "*/RDWeb/Pages/en-US/login*", "*/portal/webclient*"]
        && databytes.src > 500
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"TA0001 T1133 Citrix RD
            Gateway auth from unexpected
            source";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(cgi\/login|
            vpn\/index|LogonPoint|
            RDWeb\/Pages|
            portal\/webclient)/i";
          http.uri;
          classtype:policy-violation;
          sid:9113307; rev:1;)
    requires_variables:
        - $ALLOWED_CITRIX_IPS
        - $MPNET
    actors:
        - APT41
        - APT33
        - Ransomware
    analyst_notes: 'Citrix NetScaler/ADC, Citrix StoreFront, VMware Horizon, and RD Web Access are high-value targets - successful authentication gives adversaries GUI access to internal applications and desktops without requiring VPN. POST to login paths from unexpected source IPs indicates credential use from adversary-controlled infrastructure. Citrix CVE-2019-19781 (Shitrix) - path traversal enabling unauthenticated RCE - should also be monitored via T1190 VPN path indicators. Correlate authentication success with subsequent session activity: legitimate users open specific applications; adversaries often open full desktop sessions and initiate immediate reconnaissance.'
