# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1195.002 - Compromise Software Supply Chain
id: c741ff45-31ff-52d1-b709-e4c48b1bb69c
status: experimental
description: 'HTTPS beacon with anomalous JA4 from established software process - supply chain implant NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1195.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1195
    - attack.t1195.002
    - attack.g0016
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
    service: ssl
detection:
    selection:
        source.ip: $MPNET
        destination.port: 443
        network.packets|gte: 5
        network.packets|lte: 50
    filter_0:
        tls.client.ja4:
            - $ALLOWED_CLIENTS
            - $BROWSER_JA4
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1195
    technique_name: Supply Chain Compromise
    original_query: |-
        source.ip: $MPNET
        AND destination.port: 443
        AND NOT tls.client.ja4: (
          $ALLOWED_CLIENTS
          OR $BROWSER_JA4
        )
        AND network.packets: [5 TO 50]
    arkime: |-
        ip.src == $MPNET
        && protocols == tls
        && tls.ja3 != $ALLOWED_CLIENTS
        && tls.ja3 != $BROWSER_JA3
        && port.dst == 443
        && packets.src > 5
        && packets.src < 50
        // Process-name correlation is not available in baseline Arkime - pair with EDR/Sysmon Event 3 in the SIEM to
        // filter to suspect processes (*update*, *agent*, *service*, *.exe).
        // JA4 not available in Arkime 4.3.1 (Arkime 5+ only). Falls back to JA3 - lower entropy but still useful
        // for catching tool-vs-browser supply-chain implants.
        // See Suricata column for JA4 if your sensor supports it.
    suricata: |-
        alert tls $HOME_NET any
          -> $EXTERNAL_NET 443
          (msg:"TA0001 T1195.002 Anomalous
            JA4 from non-browser process
            supply chain beacon";
          flow:established,to_server;
          ja3.hash;
          threshold:type both,
            track by_src,
            count 5, seconds 600;
          classtype:trojan-activity;
          sid:9119506; rev:1;)
    requires_variables:
        - $ALLOWED_CLIENTS
        - $BROWSER_JA4
        - $MPNET
    dropped_clauses:
        - 'range (network.packets 5 TO 50): split into gte/lte modifiers'
    actors:
        - APT29
        - Lazarus
        - Multi
    analyst_notes: 'Supply chain implants embedded in legitimate software typically use bespoke TLS stacks (custom HTTP libraries, Go''s net/http, .NET HttpClient with custom config) that produce distinctive JA4 fingerprints unlike legitimate vendor software. Build a JA4 baseline for known-good clients in your environment - vendor software JA4 hashes are stable across versions. Implants generate JA4s outside this baseline. Pair with low-volume periodic beacon patterns (5-50 packets per session, regular intervals) to identify dormant or check-in beacons typical of supply chain implants waiting for activation. Periodicity analysis at sessions level: beacon intervals of 60s, 300s, 3600s with low jitter are characteristic.'
