# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1133 - VPN Anomalies
id: 97015e01-574b-5b62-b08e-faf12cd27f67
status: experimental
description: 'VPN authentication from unexpected geolocation - impossible travel or first-seen country NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1133
    - T1078
    - CISA advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1133
    - attack.g0016
    - attack.g0045
    - attack.g0064
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.ip: $VPN_SERVERS
        destination.port:
            - 443
            - 4433
            - 8443
            - 500
            - 4500
            - 1194
            - 1723
        source.bytes|gt: 0
    filter_0:
        source.geo.country_iso_code: $ALLOWED_COUNTRIES
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1133
    technique_name: External Remote Services
    original_query: |-
        destination.ip: $VPN_SERVERS
        AND destination.port: (
          443 OR 4433 OR 8443
          OR 500 OR 4500
          OR 1194 OR 1723
        )
        AND NOT source.geo.country_iso_code:
          $ALLOWED_COUNTRIES
        AND source.bytes > 0
    arkime: |-
        ip.dst == $VPN_SERVERS
        && protocols == [ssl, tls, udp]
        && port.dst == [443, 4433, 8443, 500, 4500, 1194, 1723]
        && ip.src != $EXTERNAL_VPN_GEOS
        && databytes.src > 0
        && databytes.dst > 0
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET
          [443,4433,8443,1194,1723]
          (msg:"TA0001 T1133 VPN auth
            from unexpected geo
            possible stolen cred";
          flow:established,to_server;
          content:"|16 03|"; depth:2;
          threshold:type both,
            track by_dst,
            count 3, seconds 60;
          classtype:policy-violation;
          sid:9113301; rev:1;)
    requires_variables:
        - $ALLOWED_COUNTRIES
        - $VPN_SERVERS
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - APT29
        - APT10
        - APT33
    analyst_notes: 'Geolocation-based VPN anomaly detection is most effective when combined with user baseline data - a user who always authenticates from the US suddenly connecting from Eastern Europe or Southeast Asia is high-confidence. Impossible travel: same account authenticating from two geographically distant locations within a timeframe physically impossible for travel (e.g., US and Russia within 2 hours). First-seen country: account authenticating from a country it has never previously used. IKEv2 uses UDP/500 and UDP/4500; SSL VPN uses TCP/443 or TCP/4433; OpenVPN uses UDP or TCP/1194; PPTP uses TCP/1723.'
