# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1566.002 - Spearphishing Link
id: 28184284-6023-5ecb-ba63-9a8cceda62d7
status: experimental
description: Credential POST to external host following phishing link click - active harvest in progress
references:
    - MITRE ATT&CK T1566.002
    - T1598.003
    - CISA advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1566
    - attack.t1566.002
    - attack.g0016
    - attack.g1015
    - attack.g0059
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: POST
        http.request.body:
            - '*password=*'
            - '*passwd=*'
            - '*pass=*'
            - '*pwd=*'
            - '*credential*'
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1566
    technique_name: Phishing
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: POST
        AND NOT destination.ip: $ALLOWED_DEFAULTS
        AND http.request.body: (
          *password=* OR *passwd=*
          OR *pass=* OR *pwd=*
          OR *credential*
        )
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == POST
        && ip.dst != $ALLOWED_DEFAULTS
        && http.reqbody == ["*password=*", "*passwd=*", "*pass=*", "*pwd=*", "*credential*", "*login*"]
        && databytes.src > 100
        && databytes.src < 2000
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0001 T1566.002 Credential
            POST to external host
            phishing harvest";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(password|passwd|
            pass|pwd|credential|
            login)=/i";
          http.request_body;
          classtype:trojan-activity;
          sid:9115605; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - Midnight Blizzard
        - Scattered Spider
        - Charming Kitten
    analyst_notes: 'A credential POST to an external unknown host is one of the highest-priority network alerts - credentials are actively being submitted to an adversary-controlled harvesting page. POST body size 100-2000 bytes covers typical credential form submissions (username + password) while filtering out large form submissions. This fires after the victim has already entered their credentials - immediate response required. Identify which user submitted, what credentials (email domain suggests the service), and whether MFA is in use. Correlate with upstream: was there a phishing click (Referer) in the minutes preceding this POST? Check for AiTM proxy patterns in the response (anomalous cookies, JA4S mismatch).'
