# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1078.002 - Domain Accounts
id: df4345c6-681d-5794-8635-a96f14c85a04
status: experimental
description: 'Password spray - low-volume auth attempts across many accounts from single external source NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1078.002
    - T1110.003
    - NSA/CISA AA20-296A
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1078
    - attack.t1078.002
    - attack.g0007
    - attack.g0016
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        http.response.status_code:
            - 401
            - 403
        url.domain:
            - '*login.microsoftonline.com*'
            - '*accounts.google.com*'
            - '*okta.com*'
        source.bytes|gt: 0
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1078
    technique_name: Valid Accounts
    original_query: |-
        NOT source.ip: $MPNET
        AND http.response.status_code: (
          401 OR 403
        )
        AND url.domain: (
          *login.microsoftonline.com*
          OR *accounts.google.com*
          OR *okta.com*
        )
        AND source.bytes > 0
    arkime: |-
        ip.src != $MPNET
        && protocols == https
        && http.method == POST
        && host.http == ["*login.microsoftonline.com*", "*accounts.google.com*", "*okta.com*", $OWA_SERVER, $ADFS_SERVER]
        && http.statuscode == [401, 403]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"TA0001 T1078.002 Password
            spray auth failures IdP
            endpoint";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(login\.microsoftonline|
            accounts\.google|
            okta\.com)/i";
          http.header;
          content:"40"; http.stat_code;
          threshold:type both,
            track by_src,
            count 5, seconds 300;
          classtype:attempted-user;
          sid:9107804; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - APT28
        - Midnight Blizzard
        - APT33
    analyst_notes: 'Password spraying uses a single password against many accounts to avoid lockout - distinguishable from brute force by low per-account attempt count (1-3) and high unique account count. Network-visible signal: many HTTP 401/403 responses from the same source IP against an IdP endpoint within a time window. Detection threshold must be tuned to your environment''s legitimate failed auth rate. Distributed sprays use many source IPs with low per-source rate - aggregate by user agent or ASN. Watch for Microsoft''s UserRealmDiscovery requests (GetCredentialType) which adversaries use to identify valid accounts before spraying.'
