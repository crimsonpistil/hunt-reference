# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1189 - Watering Hole Redirect Chains
id: 511da7bf-e836-5f54-9aad-4fa75cb162c9
status: experimental
description: Multi-hop HTTP redirect chain - exploit kit gate / traffic distribution system
references:
    - MITRE ATT&CK T1189
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1189
    - attack.g0007
    - attack.g0065
    - attack.g0032
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.response.status_code:
            - 301
            - 302
            - 303
            - 307
            - 308
        http.response.headers.location: http*
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1189
    technique_name: Drive-by Compromise
    original_query: |-
        source.ip: $MPNET
        AND http.response.status_code: (
          301 OR 302 OR 303
          OR 307 OR 308
        )
        AND http.response.headers.location:
          http*
        AND NOT destination.ip:
          $ALLOWED_DEFAULTS
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0001 T1189 Multi-hop
            redirect chain possible
            exploit kit gate";
          flow:established,to_server;
          content:"GET"; http.method;
          threshold:type both,
            track by_src,
            count 3, seconds 10;
          classtype:trojan-activity;
          sid:9118901; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - APT28
        - APT40
        - Lazarus
    analyst_notes: 'Exploit kit traffic distribution systems (TDS) gate victims through 2-5 HTTP redirects before delivering the exploit landing page - each hop profiles the victim (OS, browser, plugins) and passes only qualifying targets forward. The redirect chain has a distinctive pattern: very small response bodies (just the redirect header, no content), rapid sequential requests from the same source IP to different hosts, and a mix of HTTP 301/302 codes. Low databytes.src (<500) confirms no meaningful content was served - just redirection. Correlate the final destination of the chain with threat intel.'
