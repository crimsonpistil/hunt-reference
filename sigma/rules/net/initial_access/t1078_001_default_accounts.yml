# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1078.001 - Default Accounts
id: 5c63009f-13a5-58a5-9a22-0f7c5cdd71fe
status: experimental
description: Default credential attempt on network device / appliance management interface
references:
    - MITRE ATT&CK T1078.001
    - CISA advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1078
    - attack.t1078.001
    - attack.g1017
    - attack.g0034
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        url.path:
            - '*login*'
            - '*auth*'
            - '*cgi-bin/luci*'
            - '*api/v1/auth*'
            - '*admin*'
        http.request.body:
            - '*admin=admin*'
            - '*username=admin*'
            - '*password=1234*'
            - '*password=default*'
            - '*password=password*'
            - '*"password":"admin"*'
            - '*"password":"root"*'
            - '*"password":"password"*'
            - '*"password":"1234"*'
            - '*"password":"default"*'
            - '*"username":"admin"*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1078
    technique_name: Valid Accounts
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: POST
        AND url.path: (
          *login* OR *auth*
          OR *cgi-bin/luci*
          OR *api/v1/auth*
          OR *admin*
        )
        AND http.request.body: (
          *admin=admin*
          OR *username=admin*
          OR *password=1234*
          OR *password=default*
          OR *password=password*
          OR *"password":"admin"*
          OR *"password":"root"*
          OR *"password":"password"*
          OR *"password":"1234"*
          OR *"password":"default"*
          OR *"username":"admin"*
        )
    arkime: |-
        ip.src != $MPNET
        && protocols == http
        && http.method == POST
        && http.uri == ["*/login*", "*/auth*", "*/cgi-bin/luci*", "*/api/v1/auth*", "*/admin*"]
        && http.reqbody == ["*admin=admin*", "*username=admin&password=admin*", "*user=admin&pass=admin*", "*username=root&password=root*", "*password=1234*", "*password=default*", "*password=password*", "*\"password\":\"admin\"*", "*\"password\":\"root\"*", "*\"password\":\"password\"*", "*\"password\":\"1234\"*", "*\"password\":\"default\"*", "*\"username\":\"admin\"*", "*\"user\":\"admin\"*"]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"TA0001 T1078.001 Default
            credential attempt network
            device management";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(username|user|login)=
            (admin|root|administrator)
            .{0,20}(password|pass|pwd)=
            (admin|root|1234|default|
            password|123456)/i";
          http.request_body;
          classtype:attempted-user;
          sid:9107801; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Volt Typhoon
        - Sandworm
        - Multi
    analyst_notes: Default credentials on network devices, IP cameras, NAS, routers, and management interfaces are trivially exploited - admin/admin, admin/password, root/root, admin/1234 cover the majority of default credential sets. Focus on management interface paths from external IPs. A 200 response with a session token or redirect to a dashboard = successful default credential login. Maintain an inventory of devices with known default credentials and change them - detection is a backstop, not the primary control.
