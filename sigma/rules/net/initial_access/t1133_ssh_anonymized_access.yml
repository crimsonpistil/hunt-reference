# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1133 - SSH Anonymized Access
id: 750d5039-8c6b-5729-b916-cf2f7a44c806
status: experimental
description: 'SSH login from Tor exit node or known proxy / VPS range - anonymized initial access NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1133
    - T1090.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1133
    - attack.g0032
    - attack.g0064
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port: 22
        source.ip: $EXTERNAL_TOR_NODES
        source.bytes|gt: 1000
        destination.bytes|gt: 1000
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1133
    technique_name: External Remote Services
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 22
        AND source.ip: $EXTERNAL_TOR_NODES
        AND source.bytes > 1000
        AND destination.bytes > 1000
    arkime: |-
        ip.src != $MPNET
        && port.dst == 22
        && protocols == ssh
        && ip.src == $EXTERNAL_TOR_NODES
        || ip.src == $EXTERNAL_VPS_RANGES
        && databytes.src > 1000
        && databytes.dst > 1000
    suricata: |-
        alert tcp $EXTERNAL_TOR_NODES any
          -> $HOME_NET 22
          (msg:"TA0001 T1133 SSH login
            from Tor exit node
            anonymized access";
          flow:established,to_server;
          content:"SSH-"; depth:4;
          classtype:policy-violation;
          sid:9113306; rev:1;)
    requires_variables:
        - $EXTERNAL_TOR_NODES
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 1000): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (destination.bytes > 1000): emitted as |gt modifier, backend support varies'
    actors:
        - Lazarus
        - APT33
        - Multi
    analyst_notes: Adversaries route SSH initial access through Tor exit nodes or anonymizing VPS infrastructure (DigitalOcean, Vultr, Linode, AWS) to obscure their origin. Maintain a current Tor exit node list (updated daily from dan.me.uk/torlist or similar) in Suricata's $EXTERNAL_TOR_NODES variable. Successful SSH sessions from Tor exit nodes (bidirectional traffic, high databytes) are near-certain malicious - no legitimate administrative use case requires Tor for SSH. VPS range detection requires a threat intel feed of commonly abused hosting provider CIDR ranges.
