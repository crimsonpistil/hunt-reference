# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1133 - Cloud Management
id: 8c71b210-cf62-5a9e-ae35-9e7cd8742ce8
status: experimental
description: Cloud management API authentication from unexpected IP / new ASN - console credential abuse
references:
    - MITRE ATT&CK T1133
    - T1078.004
    - CISA advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1133
    - attack.g0016
    - attack.g0096
    - attack.g1015
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        url.domain:
            - '*console.aws.amazon.com*'
            - '*portal.azure.com*'
            - '*console.cloud.google.com*'
            - '*management.azure.com*'
        url.path:
            - '*oauth/token*'
            - '*signin*'
            - '*login*'
    filter_0:
        source.ip: $MPNET
    filter_1:
        source.ip: $ALLOWED_ADMIN_IPS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1133
    technique_name: External Remote Services
    original_query: |-
        NOT source.ip: $MPNET
        AND NOT source.ip: $ALLOWED_ADMIN_IPS
        AND http.request.method: POST
        AND url.domain: (
          *console.aws.amazon.com*
          OR *portal.azure.com*
          OR *console.cloud.google.com*
          OR *management.azure.com*
        )
        AND url.path: (
          *oauth/token* OR *signin*
          OR *login*
        )
    arkime: |-
        ip.src != $MPNET
        && ip.src != $ALLOWED_ADMIN_IPS
        && protocols == https
        && host.http == ["*console.aws.amazon.com*", "*portal.azure.com*", "*console.cloud.google.com*", "*management.azure.com*", "*ec2.amazonaws.com*"]
        && http.method == POST
        && http.uri == ["*/oauth/token*", "*/signin/oauth*", "*/login*"]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $EXTERNAL_NET 443
          (msg:"TA0001 T1133 Cloud console
            auth from unexpected IP
            credential abuse";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(console\.(aws\.amazon|
            cloud\.google)\.com|
            portal\.azure\.com|
            management\.azure\.com)/i";
          http.header;
          classtype:policy-violation;
          sid:9113308; rev:1;)
    requires_variables:
        - $ALLOWED_ADMIN_IPS
        - $MPNET
    actors:
        - APT29
        - APT41
        - Scattered Spider
    analyst_notes: 'Cloud console and API authentication from unexpected IPs or new ASNs indicates stolen credential use. Cloud management plane access is particularly high-impact - a successful console login gives adversaries access to all cloud resources including compute, storage, secrets, and IAM. Requires SSL/TLS inspection or egress proxy to detect outbound connections to cloud management URLs. Correlate with your cloud provider''s CloudTrail (AWS), Azure Activity Log, or GCP Audit Log - network-layer detection is a supplementary signal. Watch for: new AWS access key usage, Azure portal login from new country, GCP service account key download from unknown IP.'
