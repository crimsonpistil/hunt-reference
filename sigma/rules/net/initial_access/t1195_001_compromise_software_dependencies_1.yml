# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1195.001 - Compromise Software Dependencies
id: 02806aa9-f2cf-537e-87a1-fc4ef2811493
status: experimental
description: Typosquatting package fetch - internal host downloading from known typosquat namespace
references:
    - MITRE ATT&CK T1195.001
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1195
    - attack.t1195.001
    - attack.g0032
    - attack.g1036
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - '*registry.npmjs.org*'
            - '*pypi.org*'
            - '*files.pythonhosted.org*'
            - '*rubygems.org*'
            - '*crates.io*'
        url.path: $KNOWN_TYPOSQUAT_PACKAGES
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1195
    technique_name: Supply Chain Compromise
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          *registry.npmjs.org*
          OR *pypi.org*
          OR *files.pythonhosted.org*
          OR *rubygems.org*
          OR *crates.io*
        )
        AND url.path: $KNOWN_TYPOSQUAT_PACKAGES
    arkime: |-
        ip.src == $MPNET
        && protocols == https
        && host.http == ["*registry.npmjs.org*", "*pypi.org*", "*files.pythonhosted.org*", "*rubygems.org*", "*crates.io*"]
        && http.uri == ["*@*/-/*", "*/packages/*", "*/simple/*", "*/gems/*"]
        && http.uri == $KNOWN_TYPOSQUAT_PACKAGES
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET 443
          (msg:"TA0001 T1195.001 Known
            typosquat package fetch from
            public registry";
          flow:established,to_server;
          pcre:"/Host:\s*(registry\.
            npmjs\.org|pypi\.org|
            files\.pythonhosted\.org|
            rubygems\.org|
            crates\.io)/i";
          http.header;
          classtype:trojan-activity;
          sid:9119502; rev:1;)
    requires_variables:
        - $KNOWN_TYPOSQUAT_PACKAGES
        - $MPNET
    actors:
        - Lazarus
        - Moonstone Sleet
        - Multi
    analyst_notes: Typosquatting packages mimic legitimate names (reqests vs requests, electrn vs electron, lodahs vs lodash, colors-js vs colors.js). Maintain a $KNOWN_TYPOSQUAT_PACKAGES feed from threat intel sources (Snyk, Socket.dev, GitHub Advisory Database). Match against URL paths - npm uses /package-name/-/package-name-version.tgz, PyPI uses /packages/source/[hash]/[package-name]-[version].tar.gz. Also detect newly published packages that match high-target package name patterns. Internal package mirroring (Artifactory, Nexus) significantly reduces this risk - direct fetches from public registries by developer machines is the primary attack surface.
