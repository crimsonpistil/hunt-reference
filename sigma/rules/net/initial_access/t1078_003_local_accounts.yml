# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1078.003 - Local Accounts
id: 5df9e832-4d90-5d61-a95d-2ee3e7d22f92
status: experimental
description: 'Local admin credential use over SMB from external / unexpected source NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1078.003
    - T1021.002
    - CISA advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1078
    - attack.t1078.003
    - attack.g0032
    - attack.g0034
logsource:
    category: network_connection
    product: zeek
    service: smb
detection:
    selection:
        destination.port: 445
        network.transport: tcp
        smb.user:
            - '*administrator*'
            - '*admin*'
            - '*localadmin*'
        source.bytes|gt: 0
        destination.bytes|gt: 0
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1078
    technique_name: Valid Accounts
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 445
        AND network.transport: tcp
        AND smb.user: (
          *administrator*
          OR *admin*
          OR *localadmin*
        )
        AND source.bytes > 0
        AND destination.bytes > 0
    arkime: |-
        ip.src != $MPNET
        && port.dst == 445
        && protocols == smb
        && smb.user == ["*administrator*", "*admin*", "*localadmin*", "*sysadmin*"]
        && databytes.src > 0
        && databytes.dst > 0
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET 445
          (msg:"TA0001 T1078.003 SMB local
            admin auth from external
            source";
          flow:established,to_server;
          content:"|ff 53 4d 42|"; depth:5;
          classtype:policy-violation;
          sid:9107805; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (destination.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - Lazarus
        - Sandworm
        - Multi
    analyst_notes: Local administrator accounts used over SMB from external IPs indicate either a perimeter misconfiguration (SMB internet-exposed - critical) or an adversary who has obtained local credentials and is authenticating remotely. External TCP/445 should never reach internal hosts - any successful SMB session from external IPs is a P0 finding. Internally, lateral movement using local admin accounts over SMB is covered in TA0008. Zeek smb.log captures username, domain, and auth success/failure.
