# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1078.004 - Cloud Accounts
id: 1456d6a9-3534-5d18-907f-ee6f3bf7714f
status: experimental
description: Cloud service account / API key use from unexpected IP - stolen key or token abuse
references:
    - MITRE ATT&CK T1078.004
    - T1552.001
    - CISA cloud security advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.defense-evasion
    - attack.t1078
    - attack.t1078.004
    - attack.g0016
    - attack.g1015
    - attack.g0096
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        url.domain:
            - '*sts.amazonaws.com*'
            - '*oauth2.googleapis.com*'
            - '*login.microsoftonline.com*'
            - '*iam.amazonaws.com*'
        http.request.body:
            - '*client_credentials*'
            - '*AssumeRole*'
            - '*GetSessionToken*'
    filter_0:
        source.ip: $MPNET
    filter_1:
        source.ip: $EXTERNAL_CI_CD
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1078
    technique_name: Valid Accounts
    original_query: |-
        NOT source.ip: $MPNET
        AND NOT source.ip: $EXTERNAL_CI_CD
        AND http.request.method: POST
        AND url.domain: (
          *sts.amazonaws.com*
          OR *oauth2.googleapis.com*
          OR *login.microsoftonline.com*
          OR *iam.amazonaws.com*
        )
        AND http.request.body: (
          *client_credentials*
          OR *AssumeRole*
          OR *GetSessionToken*
        )
    arkime: |-
        ip.src != $MPNET
        && ip.src != $EXTERNAL_CI_CD
        && protocols == https
        && host.http == ["*sts.amazonaws.com*", "*oauth2.googleapis.com*", "*login.microsoftonline.com*", "*iam.amazonaws.com*"]
        && http.method == POST
        && http.reqbody == ["*grant_type=client_credentials*", "*grant_type=urn:ietf:params*", "*Action=AssumeRole*", "*Action=GetSessionToken*"]
        && ip.src != $ALLOWED_DEFAULTS
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $EXTERNAL_NET 443
          (msg:"TA0001 T1078.004 Cloud
            service account auth from
            unexpected IP stolen key";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(sts\.amazonaws\.com|
            oauth2\.googleapis\.com|
            login\.microsoftonline\.com|
            iam\.amazonaws\.com)/i";
          http.header;
          pcre:"/(grant_type=
            client_credentials|
            AssumeRole|
            GetSessionToken)/i";
          http.request_body;
          classtype:policy-violation;
          sid:9107806; rev:1;)
    requires_variables:
        - $EXTERNAL_CI_CD
        - $MPNET
    actors:
        - APT29
        - Scattered Spider
        - APT41
    analyst_notes: Cloud service accounts and API keys are long-lived credentials that don't require MFA - a stolen key works indefinitely until rotated. AWS STS AssumeRole and GetSessionToken from unexpected IPs indicate stolen IAM credentials. OAuth2 client_credentials grant from unknown IPs indicates stolen service account credentials. Requires egress SSL inspection to see request bodies against cloud STS endpoints. Correlate with your cloud provider's audit logs (CloudTrail, Azure Activity, GCP Audit) - network layer detection catches the authentication attempt; cloud audit logs tell you what the account did afterward.
