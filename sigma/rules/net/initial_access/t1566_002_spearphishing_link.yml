# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1566.002 - Spearphishing Link
id: 28184284-6023-5ecb-ba63-9a8cceda62d7
status: experimental
description: Internal host clicking URL in email - GET to newly registered domain within minutes of SMTP delivery
references:
    - MITRE ATT&CK T1566.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1566
    - attack.t1566.002
    - attack.g0016
    - attack.g0059
    - attack.g0094
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: GET
        http.request.headers.referer: '*mail*'
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1566
    technique_name: Phishing
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: GET
        AND http.request.headers.referer: *mail*
        AND NOT destination.ip: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == GET
        && http.hasheader.src.value == *mail*
        && ip.dst != $ALLOWED_DEFAULTS
        && http.user-agent == ["*Outlook*", "*Thunderbird*", "*Mail*", "*Chrome*", "*Firefox*"]
        // Domain-age filtering is not available in baseline Arkime 4.3.1. Pair this query with external domain-age
        // enrichment (PassiveTotal, DomainTools, RiskIQ) or filter on results manually for domains <30 days old.
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"TA0001 T1566.002 Internal
            host click to unknown domain
            possible phishing link";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/Referer:\s*https?:\/\/
            (mail|outlook|webmail|
            owa)\./i";
          http.header;
          classtype:trojan-activity;
          sid:9115604; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - Midnight Blizzard
        - Charming Kitten
        - Kimsuky
    analyst_notes: 'The Referer header reveals when a user clicked a link from a webmail interface (mail., outlook., webmail., owa.). Combining the Referer with a destination domain age check <30 days is a high-confidence phishing click indicator. Correlate forward from the click: what did the destination serve? A redirect chain, a credential harvesting page, or a payload download all follow predictably. This is one of the most actionable real-time detections - the click happens before any credentials are entered or payload executes, giving a response window. Alert immediately and correlate with inbound SMTP logs to identify the phishing email.'
