# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1195.002 - Compromise Software Supply Chain
id: c741ff45-31ff-52d1-b709-e4c48b1bb69c
status: experimental
description: 'Anomalous DNS query from updater process - DGA or sandbox evasion behavior NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1195.002
    - T1568.002
    - CISA ED-21-01
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1195
    - attack.t1195.002
    - attack.g0016
logsource:
    category: dns
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        dns.question.type:
            - A
            - AAAA
        dns.question.name|re:
            - .*avsvmcloud\.com.*
            - .*appsync\-api.*
            - '[a-z0-9]{16,}\.com'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1195
    technique_name: Supply Chain Compromise
    original_query: |-
        source.ip: $MPNET
        AND dns.question.type:
          ("A" OR "AAAA")
        AND dns.question.name: (
          *avsvmcloud.com*
          OR *appsync-api*
          OR /[a-z0-9]{16,}\.com/
        )
    arkime: |-
        ip.src == $MPNET
        && protocols == dns
        && dns.query.type == [A, AAAA]
        && host.dns == ["*avsvmcloud.com*", "*.appsync-api.*", $EXTERNAL_C2_DOMAINS]
        // Process-name correlation is not available in baseline Arkime - pair with EDR/Sysmon Event 3 in the SIEM to
        // filter to updater processes (*update*, *agent*). DGA detection requires regex - not expressible
        // in pure Arkime. See Suricata pcre column or use Kibana KQL regex syntax for runtime matching.

        // Logical spec: host.dns matches
        //   /^[a-z0-9]{16,}\.(com|net|org|info)$/
    suricata: |-
        alert dns $HOME_NET any
          -> any 53
          (msg:"TA0001 T1195.002 Anomalous
            DNS from updater DGA or
            backdoor lookup";
          flow:stateless;
          dns.query;
          pcre:"/^[a-z0-9]{16,}\.
            (com|net|org|info|us|biz)$/";
          classtype:trojan-activity;
          sid:9119505; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'mixed regex and wildcard values on dns.question.name: wildcards promoted to regex, verify intent'
    actors:
        - APT29
        - Multi
    analyst_notes: 'Supply chain backdoors often use DGA (Domain Generation Algorithms) or hardcoded callback domains for C2. SUNBURST used DGA-style subdomains under avsvmcloud.com - the algorithm hashed the victim''s domain and encoded it in the subdomain. Generic DGA detection: long alphanumeric subdomains (16+ chars) on common TLDs are statistically rare in legitimate traffic. Combine with process correlation - DGA queries from update.exe, javaupdate.exe, or vendor-named processes are high-confidence. Zeek dns.log captures all queries; build entropy-based detection on subdomain string patterns. SUNBURST specifically used a DGA that produced 16-32 character subdomains encoding the victim''s AD domain.'
