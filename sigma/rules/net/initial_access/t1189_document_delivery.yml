# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1189 - Document Delivery
id: 46bf5500-4dbb-53dc-bbe5-1de4f578b586
status: experimental
description: Malformed or exploit-bearing PDF / Office document served via HTTP from unknown host
references:
    - MITRE ATT&CK T1189
    - T1566.001
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1189
    - attack.g0065
    - attack.g0059
    - attack.g0094
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.response.status_code: 200
        http.response.headers.content-type:
            - '*application/pdf*'
            - '*application/msword*'
            - '*vnd.ms-*'
            - '*vnd.openxml*'
    filter_0:
        destination.ip: $ALLOWED_DEFAULTS
    filter_1:
        url.path:
            - '*download*'
            - '*docs*'
            - '*files*'
            - '*attachments*'
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1189
    technique_name: Drive-by Compromise
    original_query: |-
        source.ip: $MPNET
        AND http.response.status_code: 200
        AND http.response.headers.content-type: (
          *application/pdf*
          OR *application/msword*
          OR *vnd.ms-*
          OR *vnd.openxml*
        )
        AND NOT destination.ip: $ALLOWED_DEFAULTS
        AND NOT url.path: (
          *download* OR *docs*
          OR *files* OR *attachments*
        )
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.statuscode == 200
        && http.uri != ["*/download*", "*/docs/*", "*/files/*", "*/attachments/*"]
        && http.uri == ["*.pdf", "*.doc", "*.docx", "*.xls", "*.xlsx", "*.ppt", "*.pptx"]
        && ip.dst != $ALLOWED_DEFAULTS
        // Content-Type response-header inspection is not available in baseline Arkime 4.3.1. The query
        // uses URI-extension matching as a baseline-compatible alternative. For true MIME detection see Suricata
        // file_data rules or Zeek file analysis framework.
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"TA0001 T1189 Exploit doc
            served from unknown host
            possible drive-by";
          flow:established,from_server;
          content:"200"; http.stat_code;
          pcre:"/(application\/
            (pdf|msword|vnd\.ms\-|
            vnd\.openxml))/i";
          http.header;
          file_data;
          content:"|25 50 44 46|"; depth:4;
          classtype:trojan-activity;
          sid:9118907; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - APT40
        - Charming Kitten
        - Kimsuky
    analyst_notes: Drive-by attacks deliver exploit-bearing documents via HTTP - malicious PDFs (|25 50 44 46| = %PDF header), Word documents, and Excel files served from unknown hosts as part of the exploit chain. Legitimate document downloads come from known domains (SharePoint, Google Drive, your CDN) - documents served from unknown hosts in the context of a redirect chain are high-priority. Content '|25 50 44 46|' at depth 4 matches the PDF magic bytes. For Office documents check for |D0 CF 11 E0| (OLE2/legacy) or the ZIP header |50 4B 03 04| (OOXML). Zeek's files.log captures file metadata including MIME type and MD5.
