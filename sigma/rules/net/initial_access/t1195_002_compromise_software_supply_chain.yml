# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1195.002 - Compromise Software Supply Chain
id: c741ff45-31ff-52d1-b709-e4c48b1bb69c
status: experimental
description: Software updater connecting to non-vendor C2 infrastructure - Trojanized update detection
references:
    - MITRE ATT&CK T1195.002
    - CISA ED-21-01
    - Microsoft MSTIC
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1195
    - attack.t1195.002
    - attack.g0016
    - attack.g0032
    - attack.g0096
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port:
            - 443
            - 80
            - 8080
            - 8443
        process.name:
            - '*update*'
            - '*agent*'
            - '*service*'
            - '*daemon*'
    filter_0:
        destination.ip: $ALLOWED_UPDATE_SOURCES
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1195
    technique_name: Supply Chain Compromise
    original_query: |-
        source.ip: $MPNET
        AND NOT destination.ip:
          $ALLOWED_UPDATE_SOURCES
        AND destination.port: (
          443 OR 80 OR 8080 OR 8443
        )
        AND process.name: (
          *update* OR *agent*
          OR *service* OR *daemon*
        )
    arkime: |-
        ip.src == $MPNET
        && protocols != [http, https, dns]
        && ip.dst != $ALLOWED_UPDATE_SOURCES
        && port.dst == [443, 80, 8080, 8443]
        && databytes.src > 0
        && databytes.dst > 0
        // Process-name correlation is not available in baseline Arkime 4.3.1 - it sees only network sessions, not the
        // process making the connection. The network-only signal here is anomalous outbound + non-vendor destination;
        // pair with EDR/Sysmon Event 3 in the SIEM to filter to updater processes (*update*, *agent*, *service*,
        // *daemon*) for high confidence.
    suricata: |-
        alert tcp $HOME_NET any
          -> $EXTERNAL_NET
          [80,443,8080,8443]
          (msg:"TA0001 T1195.002 Software
            updater outbound to non-vendor
            infrastructure";
          flow:established,to_server;
          content:!"|16 03|"; depth:2;
          threshold:type both,
            track by_src,
            count 1, seconds 60;
          classtype:trojan-activity;
          sid:9119504; rev:1;)
    requires_variables:
        - $ALLOWED_UPDATE_SOURCES
        - $MPNET
    actors:
        - APT29
        - Lazarus
        - APT41
    analyst_notes: 'Trojanized software updates (SolarWinds Orion, 3CX desktop client, MOVEit Transfer, Asus Live Update) generate post-install C2 connections to adversary-controlled infrastructure. Network signal: a known software updater process making outbound connections to IPs/domains that aren''t part of the vendor''s known infrastructure. Requires EDR process correlation to identify the source process. Maintain a $VENDOR_UPDATE_INFRA allowlist of legitimate update server IPs/domains for installed software (avsvmcloud.com domain pattern was the SUNBURST signal). Anomalous beaconing patterns from updaters - sleep + jitter, low data volume - are characteristic of supply chain backdoors waiting for activation.'
