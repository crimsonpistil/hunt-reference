# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1133 - VPN Anomalies
id: 97015e01-574b-5b62-b08e-faf12cd27f67
status: experimental
description: 'VPN authentication outside business hours from new source IP - off-hours credential use KQL has no native time-of-day filter. To filter. to 22:00-06:00 local, define an Elasticsearch. runtime field that derives hour_of_day from. @timestamp, then use AND hour_of_day >= 22 OR. hour_of_day < 6, OR apply via Lens/dashboard. time-bucket filtering. NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1133
    - T1078
    - CISA advisories
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1133
    - attack.g0016
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.ip: $VPN_SERVERS
        destination.port:
            - 443
            - 4433
            - 8443
        source.bytes|gt: 1000
    filter_0:
        source.ip: $EXTERNAL_VPN_IPS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1133
    technique_name: External Remote Services
    original_query: |-
        destination.ip: $VPN_SERVERS
        AND destination.port: (
          443 OR 4433 OR 8443
        )
        AND NOT source.ip: $EXTERNAL_VPN_IPS
        AND source.bytes > 1000
    arkime: |-
        ip.dst == $VPN_SERVERS
        && protocols == tls
        && port.dst == [443, 4433, 8443]
        && ip.src != $EXTERNAL_VPN_IPS
        && databytes.src > 1000
        && databytes.dst > 1000
        // Time-of-day filtering (hour > 22 || hour < 6) is
        // not expressible in the Arkime query language. Use
        // the Arkime UI timeframe selector for off-hours
        // windows, or apply the time filter at SIEM
        // correlation time. Suricata threshold below catches
        // the new-source-IP burst regardless of hour.
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET [443,4433,8443]
          (msg:"TA0001 T1133 VPN auth
            off-hours new source IP";
          flow:established,to_server;
          content:"|16 03|"; depth:2;
          content:"|01|"; offset:5;
          depth:1;
          threshold:type both,
            track by_src,
            count 1, seconds 300;
          classtype:policy-violation;
          sid:9113302; rev:1;)
    requires_variables:
        - $EXTERNAL_VPN_IPS
        - $VPN_SERVERS
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 1000): emitted as |gt modifier, backend support varies'
    actors:
        - Midnight Blizzard
        - Lazarus
        - Multi
    analyst_notes: Off-hours VPN connections from new source IPs combine two anomaly signals into a higher-confidence indicator. Adversaries authenticating with stolen credentials often do so outside business hours to reduce detection risk and analyst response time. Content '|16 03|' = TLS record header, '|01|' at offset 5 = ClientHello - identifies an active TLS handshake initiation. Build a per-account VPN source IP baseline - the first connection from a new IP address during off-hours (22:00-06:00 local) warrants investigation regardless of successful authentication.
