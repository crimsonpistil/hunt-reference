# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1190 - Web App Injection
id: 8541bfa6-d23c-55ae-8bb4-f7a270a5086e
status: experimental
description: Path traversal / LFI - directory traversal attempt to read sensitive files
references:
    - MITRE ATT&CK T1190
    - CISA advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1190
    - attack.g0065
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        http.request.method: GET
        url.path:
            - '*../../../*'
            - '*..%2F*'
            - '*..%5C*'
            - '*%2e%2e%2f*'
            - '*/etc/passwd*'
            - '*win.ini*'
            - '*web.config*'
            - '*.htaccess*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1190
    technique_name: Exploit Public-Facing Application
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: GET
        AND url.path: (
          *../../../* OR *..%2F*
          OR *..%5C* OR *%2e%2e%2f*
          OR */etc/passwd*
          OR *win.ini* OR *web.config*
          OR *.htaccess*
        )
    arkime: |-
        ip.src != $MPNET
        && protocols == http
        && http.method == GET
        && http.uri == ["*../../../*", "*..%2F*", "*..%5C*", "*%2e%2e%2f*", "*%252e%252e*", "*/etc/passwd*", "*/windows/win.ini*", "*/proc/self/environ*", "*web.config*", "*.htaccess*"]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"TA0001 T1190 Path traversal
            LFI attempt";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/(\.\.[\/\\]|
            \%2[eE]\%2[eE]\%2[fF5cC]|
            \%252[eE]|
            \/etc\/passwd|
            \/windows\/win\.ini|
            \/proc\/self|
            web\.config|
            \.htaccess)/i";
          http.uri;
          classtype:web-application-attack;
          sid:9119003; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT40
        - APT33
        - Multi
    analyst_notes: 'Path traversal (../) reads files outside the web root - /etc/passwd confirms a Unix system and reveals usernames, /proc/self/environ reveals environment variables including credentials, web.config and .htaccess reveal application configuration including database credentials. Double URL encoding (%252e%252e = ../) bypasses WAFs that only decode once. A 200 response to a path traversal request containing root:x:0:0 or [system] (win.ini marker) = successful LFI exploitation. High-priority: LFI reading /proc/self/environ or web.config means credentials may be exposed in the response body visible in Zeek http.log or Arkime session data.'
