# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/initial_access.js
title: T1195.001 - Compromise Software Dependencies
id: 02806aa9-f2cf-537e-87a1-fc4ef2811493
status: experimental
description: 'Build server / CI/CD agent making unexpected outbound connection - dependency exfil or backdoor C2 NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1195.001
    - CISA ED-21-01
    - NIST SP 800-161
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.initial-access
    - attack.t1195
    - attack.t1195.001
    - attack.g0016
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $BUILD_SERVERS
        destination.port:
            - 443
            - 80
            - 8443
            - 4444
            - 1337
            - 8080
            - 9999
        source.bytes|gt: 0
    filter_0:
        destination.ip:
            - $MPNET
            - $EXTERNAL_PACKAGE_REGISTRIES
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/initial_access.js
    domain: net
    technique: T1195
    technique_name: Supply Chain Compromise
    original_query: |-
        source.ip: $BUILD_SERVERS
        AND NOT destination.ip: (
          $MPNET OR
          $EXTERNAL_PACKAGE_REGISTRIES
        )
        AND destination.port: (
          443 OR 80 OR 8443
          OR 4444 OR 1337
          OR 8080 OR 9999
        )
        AND source.bytes > 0
    arkime: |-
        ip.src == $BUILD_SERVERS
        && protocols != [http, https, dns, ntp, git, ldap, syslog]
        && ip.dst != $MPNET
        && ip.dst != $EXTERNAL_PACKAGE_REGISTRIES
        && port.dst == [443, 80, 8443, 4444, 1337, 8080, 9999]
        && databytes.src > 0
    suricata: |-
        alert tcp $BUILD_SERVERS any
          -> $EXTERNAL_NET
          [80,443,8443,4444,1337,8080]
          (msg:"TA0001 T1195.001 Build
            server unexpected outbound
            possible dep compromise";
          flow:established,to_server;
          content:!"|16 03|"; depth:2;
          content:!"GET "; depth:5;
          content:!"POST "; depth:6;
          threshold:type both,
            track by_src,
            count 1, seconds 60;
          classtype:trojan-activity;
          sid:9119501; rev:1;)
    requires_variables:
        - $BUILD_SERVERS
        - $EXTERNAL_PACKAGE_REGISTRIES
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - APT29
        - Lazarus
        - Multi
    analyst_notes: Build servers (Jenkins, GitLab CI, GitHub Actions runners, Azure DevOps agents) have predictable network behavior - they pull from package registries (npm, PyPI, Maven, NuGet, Docker Hub) and source repositories, push artifacts to internal storage, and report back to orchestrators. Any outbound connection to non-package-registry destinations is anomalous. Compromised dependencies often beacon out from build agents during the build process - the malicious code runs in the CI environment with full credentials. Maintain $EXTERNAL_PACKAGE_REGISTRIES allowlist (registry.npmjs.org, pypi.org, maven.apache.org, *.docker.io, hub.docker.com, ghcr.io). Combine with EDR process data - node_modules postinstall scripts and pip install hooks are common compromise points.
