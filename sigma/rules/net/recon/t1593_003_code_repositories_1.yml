# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1593.003 - Code Repositories
id: 5196be74-0d0d-5630-a6ae-d302e07e1ed1
status: experimental
description: 'git clone over HTTPS - bulk repository cloning from suspicious endpoint NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1593.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1593
    - attack.t1593.003
    - attack.g0096
    - attack.g0016
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - github.com
            - gitlab.com
            - bitbucket.org
        user_agent.original: '*git/*'
        url.path:
            - '*info/refs*'
            - '*git-upload-pack*'
        destination.bytes|gt: 100000
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1593
    technique_name: Search Open Websites / Domains
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          "github.com" OR "gitlab.com"
          OR "bitbucket.org"
        )
        AND user_agent.original: *git/*
        AND url.path: (
          *info/refs*
          OR *git-upload-pack*
        )
        AND destination.bytes > 100000
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && host.http == ["*github.com*", "*gitlab.com*", "*bitbucket.org*"]
        && http.user-agent == *git/*
        && http.uri == ["*/info/refs*", "*/git-upload-pack*"]
        && databytes.dst > 100000
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1593.003 Git clone
            bulk repo download";
          flow:established,to_server;
          content:"git/"; http.header;
          content:"/info/refs"; http.uri;
          threshold:type both,
            track by_src,
            count 5, seconds 300;
          classtype:policy-violation;
          sid:9159306; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (destination.bytes > 100000): emitted as |gt modifier, backend support varies'
    actors:
        - APT41
        - APT29
        - Multi
    analyst_notes: 'Git clone over HTTPS generates a two-step HTTP exchange: GET /repo.git/info/refs?service=git-upload-pack (discovery) followed by POST /repo.git/git-upload-pack (pack download). User-Agent is always git/[version] - unforgeable without breaking git protocol. Bulk cloning of multiple repos from your org''s namespace from an endpoint that isn''t a known CI/CD system is anomalous. Large databytes.dst reflects repository size.'
