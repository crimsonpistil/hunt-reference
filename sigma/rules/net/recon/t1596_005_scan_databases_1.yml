# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1596.005 - Scan Databases
id: b99d17e3-9ac6-54e2-9951-da9a3d72861c
status: experimental
description: VirusTotal / OTX / URLScan - own infrastructure submitted to threat intel platform
references:
    - MITRE ATT&CK T1596.005
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1596
    - attack.t1596.005
    - attack.g0032
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - virustotal.com
            - otx.alienvault.com
            - urlscan.io
            - urlvoid.com
        url.path:
            - '*api/v3/domains*'
            - '*api/v3/ip_addresses*'
            - '*api/v1/indicators*'
            - '*result*'
            - '*scan*'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1596
    technique_name: Search Technical Databases
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          "virustotal.com"
          OR "otx.alienvault.com"
          OR "urlscan.io"
          OR "urlvoid.com"
        )
        AND url.path: (
          *api/v3/domains*
          OR *api/v3/ip_addresses*
          OR *api/v1/indicators*
          OR *result* OR *scan*
        )
    arkime: |-
        ip.src == $MPNET
        && host.http == ["*virustotal.com*", "*otx.alienvault.com*", "*urlscan.io*", "*urlvoid.com*"]
        && http.method == [GET, POST]
        && http.uri == ["*/api/v3/domains/*", "*/api/v3/ip_addresses/*", "*/api/v1/indicators/*", "*/result/*", "*/scan*"]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1596.005 Internal
            host submitting own infra to
            threat intel platform";
          flow:established,to_server;
          pcre:"/Host\s*:\s*(
            virustotal\.com|
            otx\.alienvault\.com|
            urlscan\.io|
            urlvoid\.com)/ix";
          http.header;
          classtype:policy-violation;
          sid:9159407; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Lazarus
        - Multi
    analyst_notes: Submitting your own domain or IP to VirusTotal from an internal host leaks intelligence - VT results are visible to all paid subscribers. A compromised host checking if infrastructure is flagged = adversary verifying C2 isn't burned. URLScan.io takes public screenshots of submitted URLs - submitting an internal URL creates a permanent public screenshot of your internal web applications. Critical OPSEC failure if observed from unexpected endpoints.
