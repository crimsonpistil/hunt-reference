# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.002 - Software
id: ce4e8096-fc6c-58e7-aa05-60cea3161b37
status: experimental
description: 'SMB protocol negotiation - OS and software version fingerprinting NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1592.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.002
    - attack.g0007
    - attack.g0045
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port: 445
        network.transport: tcp
        source.bytes|gt: 0
        destination.bytes|gt: 0
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 445
        AND network.transport: tcp
        AND source.bytes > 0
        AND destination.bytes > 0
    arkime: |-
        ip.src != $MPNET
        && port.dst == 445
        && protocols == smb
        && databytes.src > 0
        && databytes.dst > 0
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET 445
          (msg:"RECON T1592.002 SMB
            negotiate software fingerprint";
          flow:established,to_server;
          content:"|ff 53 4d 42|"; depth:5;
          classtype:attempted-recon;
          sid:9159211; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (destination.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - APT28
        - APT10
        - Lazarus
    analyst_notes: SMB protocol negotiation reveals OS version, SMB dialect (SMB1/2/3), and build number - enough to identify Windows version down to patch level without authentication. Content '|ff 53 4d 42|' = SMB1 header magic; SMB2 uses '|fe 53 4d 42|'. External TCP/445 should never reach internal hosts. Zeek smb.log captures dialect negotiation details.
