# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1596.004 - Certificate Transparency
id: 264cbc30-401c-5070-9b5b-59f29104b084
status: experimental
description: CT stream monitoring - real-time WebSocket feed for newly issued cert tracking
references:
    - MITRE ATT&CK T1596.004
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1596
    - attack.t1596.004
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - certstream.calidog.io
            - ct.cloudflare.com
            - mammoth.ct.comodo.com
        network.protocol: websocket
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1596
    technique_name: Search Technical Databases
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          "certstream.calidog.io"
          OR "ct.cloudflare.com"
          OR "mammoth.ct.comodo.com"
        )
        AND network.protocol: "websocket"
    arkime: |-
        ip.src == $MPNET
        && host.http == ["*certstream.calidog.io*", "*ct.cloudflare.com*", "*mammoth.ct.comodo.com*"]
        && protocols == wss
        && databytes.dst > 0
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1596.004 Internal
            host CT stream monitoring
            websocket";
          flow:established,to_server;
          content:"certstream"; http.uri;
          content:"Upgrade: websocket";
          http.header;
          classtype:policy-violation;
          sid:9159405; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Multi
    analyst_notes: CertStream provides a real-time WebSocket feed of all newly issued CT log certificates. A persistent WebSocket connection to certstream.calidog.io from an endpoint that isn't a known security monitoring system is anomalous - it's a running process. Adversaries can use this feed to monitor when you issue new certificates, revealing new services and infrastructure as they come online. Relatively niche but high-signal when it fires from unexpected endpoints.
