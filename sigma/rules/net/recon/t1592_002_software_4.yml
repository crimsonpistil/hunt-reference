# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.002 - Software
id: ce4e8096-fc6c-58e7-aa05-60cea3161b37
status: experimental
description: 'RDP protocol negotiation - software version and NLA configuration fingerprinting NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1592.002
    - CISA ransomware advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.002
    - attack.g0032
    - attack.g0007
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port: 3389
        network.transport: tcp
        source.bytes|gt: 0
        destination.bytes|gt: 0
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 3389
        AND network.transport: tcp
        AND source.bytes > 0
        AND destination.bytes > 0
    arkime: |-
        ip.src != $MPNET
        && port.dst == 3389
        && protocols == rdp
        && databytes.src > 0
        && databytes.dst > 0
        && packets.src < 5
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET 3389
          (msg:"RECON T1592.002 RDP
            software/NLA config fingerprint";
          flow:established,to_server;
          content:"|03 00|"; depth:2;
          classtype:attempted-recon;
          sid:9159212; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (destination.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - Lazarus
        - APT28
        - Multi
    analyst_notes: 'RDP connection negotiation reveals whether NLA is enforced, RDP protocol version, and supported encryption levels - before any authentication. Connect-and-disconnect with low packet count (<5) from external IP is the classic RDP fingerprint pattern. Content ''|03 00|'' matches TPKT header. Tools: rdp-sec-check, nmap RDP scripts. NLA disabled = pre-auth attack surface (BlueKeep CVE-2019-0708).'
