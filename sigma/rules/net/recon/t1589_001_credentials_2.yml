# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1589.001 - Credentials
id: 718ffd84-ba64-52b5-9dd1-ff1ea90a53ca
status: experimental
description: OWA / EWS user enumeration via timed response differential
references:
    - MITRE ATT&CK T1589.001
    - CISA AA21-116A
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1589
    - attack.t1589.001
    - attack.g0045
    - attack.g0064
    - attack.g0007
    - attack.g0094
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        url.path:
            - '*owa/auth.owa*'
            - '*EWS/Exchange.asmx*'
            - '*/ews/*'
        http.response.status_code:
            - 401
            - 403
            - 200
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1589
    technique_name: Gather Victim Identity Information
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: POST
        AND url.path: (
          *owa/auth.owa*
          OR *EWS/Exchange.asmx*
          OR */ews/*
        )
        AND http.response.status_code:
          (401 OR 403 OR 200)
    arkime: |-
        ip.src != $MPNET
        && http.method == POST
        && host.http == ["*owa*", "*webmail*", "*exchange*"]
        && http.uri == ["*/owa/auth.owa*", "*/EWS/Exchange.asmx*"]
        && http.statuscode == [401, 403, 200]
        && packets.src > 10
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1589.001 OWA/EWS
            auth endpoint enum";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(owa\/auth\.owa|
            EWS\/Exchange\.asmx|
            ews\/exchange)/ix";
          http.uri;
          threshold:type both,
            track by_src, count 5,
            seconds 60;
          classtype:attempted-user;
          sid:9158903; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT10
        - APT33
        - APT28
        - Kimsuky
    analyst_notes: OWA returns subtly different responses for valid vs invalid usernames - timing oracle. EWS abused by MailSniper and ruler for both enumeration and post-auth data collection. Monitor sustained POST volume outside business hours especially. A patient operator at 1 request/30 seconds will slip under the threshold - supplement with 24-hour cumulative Kibana query.
