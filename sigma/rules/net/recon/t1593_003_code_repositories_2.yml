# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1593.003 - Code Repositories
id: 5196be74-0d0d-5630-a6ae-d302e07e1ed1
status: experimental
description: Self-hosted GitLab / Bitbucket SCM API enumeration - external repository listing
references:
    - MITRE ATT&CK T1593.003
    - FBI/CISA advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1593
    - attack.t1593.003
    - attack.g0096
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        url.domain:
            - '*gitlab.<YOUR_DOMAIN>*'
            - '*bitbucket.<YOUR_DOMAIN>*'
            - '*git.<YOUR_DOMAIN>*'
        url.path:
            - '*/api/v4/projects*'
            - '*/rest/api/1.0/repos*'
            - '*/explore/repos*'
            - '*/api/v4/users*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1593
    technique_name: Search Open Websites / Domains
    original_query: |-
        NOT source.ip: $MPNET
        AND url.domain: (
          *gitlab.<YOUR_DOMAIN>*
          OR *bitbucket.<YOUR_DOMAIN>*
          OR *git.<YOUR_DOMAIN>*
        )
        AND url.path: (
          */api/v4/projects*
          OR */rest/api/1.0/repos*
          OR */explore/repos*
          OR */api/v4/users*
        )
    arkime: |-
        ip.src != $MPNET
        && host.http == ["*gitlab.<YOUR_DOMAIN>*", "*bitbucket.<YOUR_DOMAIN>*", "*git.<YOUR_DOMAIN>*"]
        && http.method == GET
        && http.uri == ["*/api/v4/projects*", "*/rest/api/1.0/repos*", "*/explore/repos*", "*/api/v4/users*"]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1593.003 External
            internal SCM API enumeration";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/(api\/v4\/projects|
            rest\/api\/1\.0\/repos|
            explore\/repos|
            api\/v4\/users)/ix";
          http.uri;
          classtype:attempted-recon;
          sid:9159307; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - APT33
        - Multi
    analyst_notes: Self-hosted GitLab and Bitbucket expose REST APIs that list all repositories and users - even without authentication if misconfigured with public visibility. /api/v4/projects returns all repositories an unauthenticated user can see. External access to these endpoints from unknown IPs = repository enumeration. A 200 response with a large JSON payload from an external IP = critical misconfiguration - your repo list is public.
