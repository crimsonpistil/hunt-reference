# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1589.002 - Email Addresses
id: 0ec1c002-0ff1-52fe-88d8-73acf7798628
status: experimental
description: 'Web scraping of staff directory / contact pages for email harvesting NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1589.002
    - T1591.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1589
    - attack.t1589.002
    - attack.g0059
    - attack.g0094
    - attack.g0045
    - attack.g0016
logsource:
    category: proxy
detection:
    selection:
        http.request.method: GET
        url.path:
            - '*staff*'
            - '*team*'
            - '*people*'
            - '*directory*'
            - '*contact*'
            - '*about*'
            - '*leadership*'
            - '*board*'
            - '*faculty*'
        http.response.bytes|gt: 50000
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1589
    technique_name: Gather Victim Identity Information
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: GET
        AND url.path: (
          *staff* OR *team*
          OR *people* OR *directory*
          OR *contact* OR *about*
          OR *leadership* OR *board*
          OR *faculty*
        )
        AND http.response.bytes > 50000
    arkime: |-
        ip.src != $MPNET
        && http.method == GET
        && http.uri == ["*/staff*", "*/team*", "*/people*", "*/directory*", "*/contact*", "*/about*", "*/leadership*", "*/board*", "*/faculty*", "*/experts*"]
        && databytes.src > 50000
        && packets.src > 40
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1589.002 Staff
            directory scraping";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/\/(staff|team|people|
            directory|contact|about|
            leadership|board|faculty|
            experts)/ix";
          http.uri;
          threshold:type both,
            track by_src,
            count 20, seconds 60;
          classtype:web-application-activity;
          sid:9158908; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (http.response.bytes > 50000): emitted as |gt modifier, backend support varies'
    actors:
        - Charming Kitten
        - Kimsuky
        - APT10
        - Cozy Bear
    analyst_notes: High-volume GETs to people/directory pages from single IP with large response bytes = automated scraping. Correlate UA - Python-requests, HeadlessChrome, PhantomJS common. Adversaries derive email format (first.last@, flast@) from scraped names then validate against cloud IdP endpoints. Directory scraping → cloud IdP validation is a documented two-step chain.
