# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.003 - Spearphishing Link
id: 319a68c9-b4f8-51a1-9325-a1cddb4e3683
status: experimental
description: Spearphishing link - internal host clicking newly registered suspicious domain
references:
    - MITRE ATT&CK T1598.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.003
    - attack.g0007
    - attack.g0094
    - attack.g0128
    - attack.g1036
logsource:
    category: network_connection
    product: zeek
    service: ssl
detection:
    selection:
        source.ip: $MPNET
        url.path:
            - '*login*'
            - '*verify*'
            - '*secure*'
            - '*account*'
            - '*update*'
            - '*confirm*'
            - '*signin*'
            - '*auth*'
        tls.server.not_before: '[now-30d TO now]'
    filter_0:
        url.domain: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        source.ip: $MPNET
        AND NOT url.domain: $ALLOWED_DEFAULTS
        AND url.path: (
          *login* OR *verify*
          OR *secure* OR *account*
          OR *update* OR *confirm*
          OR *signin* OR *auth*
        )
        AND tls.server.not_before:
          [now-30d TO now]
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.method == GET
        && host.http != $ALLOWED_DEFAULTS
        && host.http == ["*login*", "*verify*", "*secure*", "*account*", "*signin*", "*auth*", "*microsoft*", "*office365*"]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1598.003 Click to
            suspicious login domain";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/(login|verify|secure|
            account|update|confirm|
            signin|auth)/i";
          http.uri;
          classtype:social-engineering;
          sid:9159801; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - APT28
        - Kimsuky
        - ZIRCONIUM
        - Moonstone Sleet
    analyst_notes: Correlate domain age via threat intel (WHOIS, PassiveDNS). Domains registered within 30 days containing auth-themed keywords are high-risk. Typosquats (micros0ft, g00gle, rn-icrosoft) require fuzzy matching against your known-good list.
