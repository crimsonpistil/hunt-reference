# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1591.001 - Physical Locations
id: ab9eec47-4d73-553e-bb87-c0f27cd4b2c3
status: experimental
description: 'HR scraper bots hitting career / about / team pages NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1591
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1591
    - attack.t1591.001
    - attack.g0007
    - attack.g0032
    - attack.g1017
    - attack.g0046
logsource:
    category: proxy
detection:
    selection:
        url.path:
            - '*careers*'
            - '*about*'
            - '*team*'
            - '*recruit*'
            - '*apply*'
            - '*join*'
        http.response.bytes|gt: 50000
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1591
    technique_name: Gather Victim Org Information
    original_query: |-
        NOT source.ip: $MPNET
        AND url.path: (
          *careers* OR *about*
          OR *team* OR *recruit*
          OR *apply* OR *join*
        )
        AND http.response.bytes > 50000
    arkime: |-
        ip.src != $MPNET
        && http.uri == ["*careers*", "*recruit*", "*apply*", "*about*", "*team*", "*join*"]
        && databytes.src > 50000
        && packets.src > 30
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1591 Org scrape
            HR page high volume";
          flow:established,to_server;
          content:"GET"; http.method;
          content:"/careers"; http.uri;
          threshold:type threshold,
            track by_src,
            count 20, seconds 60;
          classtype:web-application-activity;
          sid:9159101; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (http.response.bytes > 50000): emitted as |gt modifier, backend support varies'
    actors:
        - APT28
        - Lazarus
        - Volt Typhoon
        - FIN7
    analyst_notes: High-volume GETs to /careers, /about, /team from single external IP in short window. Correlate UA - Python-requests, curl, headless browser UAs (HeadlessChrome, PhantomJS) common. Adversaries derive org chart, tech stack, and high-value target roles from job descriptions.
