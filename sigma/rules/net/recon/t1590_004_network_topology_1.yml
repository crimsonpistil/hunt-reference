# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1590.004 - Network Topology
id: f096d6d7-8374-5726-a4e6-d1f657331709
status: experimental
description: 'UDP traceroute - high-port probing (33434-33534) NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1590.004
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1590
    - attack.t1590.004
    - attack.g0034
    - attack.g0096
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        network.transport: udp
        destination.port|gte: 33434
        destination.port|lte: 33534
        destination.bytes: 0
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1590
    technique_name: Gather Victim Network Information
    original_query: |-
        NOT source.ip: $MPNET
        AND network.transport: udp
        AND destination.port:
          [33434 TO 33534]
        AND destination.bytes: 0
    arkime: |-
        ip.src != $MPNET
        && protocols == udp
        && port.dst >= 33434
        && port.dst <= 33534
        && databytes.src == 0
    suricata: |-
        alert udp $EXTERNAL_NET any
          -> $HOME_NET 33434:33534
          (msg:"RECON T1590.004 UDP
            traceroute high-port probe";
          dsize:0;
          threshold:type both,
            track by_src, count 5,
            seconds 15;
          classtype:attempted-recon;
          sid:9159006; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'range (destination.port 33434 TO 33534): split into gte/lte modifiers'
    actors:
        - Sandworm
        - APT41
        - Multi
    analyst_notes: Classic Unix traceroute uses UDP 33434+ with incrementing TTL. Zero-byte payload distinguishes traceroute probes from legitimate UDP services. Windows traceroute uses ICMP echo by default - cover both. tcptraceroute (TCP SYN to 80/443) requires TTL pattern analysis.
