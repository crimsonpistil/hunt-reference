# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1596.004 - Certificate Transparency
id: 264cbc30-401c-5070-9b5b-59f29104b084
status: experimental
description: Certificate Transparency log query - CT log scraping for org subdomain enumeration
references:
    - MITRE ATT&CK T1596.004
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1596
    - attack.t1596.004
    - attack.g0096
    - attack.g0007
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - crt.sh
            - certspotter.com
            - sslmate.com
            - censys.io
            - ct.googleapis.com
        url.path:
            - '*?q=*'
            - '*search*'
            - '*api/v1/certs*'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1596
    technique_name: Search Technical Databases
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          "crt.sh" OR "certspotter.com"
          OR "sslmate.com"
          OR "censys.io"
          OR "ct.googleapis.com"
        )
        AND url.path: (
          *?q=* OR *search*
          OR *api/v1/certs*
        )
    arkime: |-
        ip.src == $MPNET
        && host.http == ["*crt.sh*", "*certspotter.com*", "*sslmate.com*", "*transparencyreport.google.com*", "*censys.io*"]
        && http.method == GET
        && http.uri == ["*/?q=*", "*/search*", "*/api/v1/certs*"]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1596.004 Internal
            host CT log subdomain query";
          flow:established,to_server;
          pcre:"/Host\s*:\s*(
            crt\.sh|certspotter\.com|
            sslmate\.com|
            ct\.googleapis\.com|
            censys\.io)/ix";
          http.header;
          classtype:policy-violation;
          sid:9159404; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - APT28
        - APT33
    analyst_notes: Certificate Transparency logs record every TLS certificate issued for your domain - including wildcard certs revealing subdomain patterns, SAN entries listing internal hostnames, and certs for decommissioned services. A crt.sh query for %.<YOUR_DOMAIN> returns every cert ever issued. CT logs are public and queryable without authentication - adversaries use this as a zero-noise subdomain enumeration technique that never touches your infrastructure.
