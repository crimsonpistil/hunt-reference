# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1589.002 - Email Addresses
id: 0ec1c002-0ff1-52fe-88d8-73acf7798628
status: experimental
description: O365 / Google Workspace email format validation via login page
references:
    - MITRE ATT&CK T1589.002
    - Microsoft MSTIC
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1589
    - attack.t1589.002
    - attack.g0059
    - attack.g0016
    - attack.g0094
    - attack.g0128
logsource:
    category: proxy
detection:
    selection:
        url.domain:
            - login.microsoftonline.com
            - accounts.google.com
        url.path:
            - '*GetCredentialType*'
            - '*signin/sl/lookup*'
            - '*signin/v2/challenge*'
        http.request.method: POST
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1589
    technique_name: Gather Victim Identity Information
    original_query: |-
        NOT source.ip: $MPNET
        AND url.domain: (
          "login.microsoftonline.com"
          OR "accounts.google.com"
        )
        AND url.path: (
          *GetCredentialType*
          OR *signin/sl/lookup*
          OR *signin/v2/challenge*
        )
        AND http.request.method: POST
    arkime: |-
        ip.src != $MPNET
        && http.method == POST
        && host.http == ["*login.microsoftonline.com*", "*accounts.google.com*"]
        && http.uri == ["*/common/GetCredentialType*", "*/_/signin/sl/lookup*", "*/signin/v2/challenge*"]
        && packets.src > 10
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1589.002 Cloud IdP
            email address enumeration";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(GetCredentialType|
            signin\/sl\/lookup|
            signin\/v2\/challenge)/ix";
          http.uri;
          threshold:type both,
            track by_src,
            count 15, seconds 60;
          classtype:attempted-recon;
          sid:9158907; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Charming Kitten
        - Midnight Blizzard
        - Kimsuky
        - ZIRCONIUM
    analyst_notes: Google's /_/signin/sl/lookup returns different responses for registered vs unregistered addresses. Combined with GetCredentialType for O365, adversaries validate entire LinkedIn-harvested employee lists against both platforms in minutes. Monitor via CASB/proxy for internal hosts performing this in bulk.
