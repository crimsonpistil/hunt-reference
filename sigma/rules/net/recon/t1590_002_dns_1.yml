# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1590.002 - DNS
id: b9996a16-2039-54e0-86ac-5950e6ca9bf0
status: experimental
description: Bulk subdomain brute-force / DNS enumeration
references:
    - MITRE ATT&CK T1590.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1590
    - attack.t1590.002
    - attack.g0096
    - attack.g0007
    - attack.g0064
    - attack.g1017
logsource:
    category: dns
    product: zeek
detection:
    selection:
        dns.question.type: A
        dns.question.name: '*.<YOUR_DOMAIN>'
    filter_0:
        source.ip: $MPNET
    filter_1:
        dns.resolved_ip: '*'
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1590
    technique_name: Gather Victim Network Information
    original_query: |-
        NOT source.ip: $MPNET
        AND dns.question.type: "A"
        AND dns.question.name:
          *.<YOUR_DOMAIN>
        AND NOT dns.resolved_ip: *
    arkime: |-
        ip.src != $MPNET
        && protocols == dns
        && dns.query.type == A
        && host.dns == "*.<YOUR_DOMAIN>"
        && packets.src > 50
    suricata: |-
        alert dns $EXTERNAL_NET any
          -> $DNS_SERVERS any
          (msg:"RECON T1590.002 Subdomain
            brute-force enumeration";
          dns.query;
          pcre:"/^[a-z0-9\-]{2,30}
            \.<YOUR_DOMAIN>\.com$/i";
          threshold:type both,
            track by_src,
            count 20, seconds 30;
          classtype:attempted-recon;
          sid:9159002; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - APT28
        - APT33
        - Volt Typhoon
    analyst_notes: 'Tools: dnsx, amass, subfinder, fierce. High NXDOMAIN ratio from single source IP is strongest signal - correlate query count vs NXDOMAIN response count. 70%+ NXDOMAIN rate from one source over 60 seconds = near-certain brute force. Watch slow-and-low variants staying under per-minute thresholds.'
