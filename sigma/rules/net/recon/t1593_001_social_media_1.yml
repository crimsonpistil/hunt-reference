# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1593.001 - Social Media
id: a90a35ce-30d2-5fa5-810b-9829ebb0c0b1
status: experimental
description: 'Social platform bulk org mention queries from internal host NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1593.001
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1593
    - attack.t1593.001
    - attack.g0094
    - attack.g0016
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - twitter.com
            - x.com
            - reddit.com
            - glassdoor.com
            - facebook.com
        url.path:
            - '*search*'
            - '*query*'
            - '*api/search*'
            - '*graphql*'
        http.response.bytes|gt: 10000
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1593
    technique_name: Search Open Websites / Domains
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          "twitter.com" OR "x.com"
          OR "reddit.com"
          OR "glassdoor.com"
          OR "facebook.com"
        )
        AND url.path: (
          *search* OR *query*
          OR *api/search*
          OR *graphql*
        )
        AND http.response.bytes > 10000
    arkime: |-
        ip.src == $MPNET
        && host.http == ["*twitter.com*", "*x.com*", "*reddit.com*", "*glassdoor.com*", "*facebook.com*"]
        && http.uri == ["*/search*", "*/query*", "*/api/search*", "*/graphql*"]
        && databytes.src > 10000
        && packets.src > 15
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1593.001 Internal
            social platform bulk search";
          flow:established,to_server;
          pcre:"/Host\s*:\s*(
            twitter\.com|x\.com|
            reddit\.com|glassdoor\.com|
            facebook\.com)/ix";
          http.header;
          threshold:type both,
            track by_src,
            count 15, seconds 60;
          classtype:policy-violation;
          sid:9159302; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (http.response.bytes > 10000): emitted as |gt modifier, backend support varies'
    actors:
        - Kimsuky
        - Cozy Bear
        - Multi
    analyst_notes: Adversaries query social platforms for mentions of your org, employee names, and internal tool names. Reddit contains employee posts mentioning internal tools and outages. Glassdoor reviews often disclose internal technology stacks in detail. GraphQL endpoints on Twitter/X and Facebook are used by automated tools to bulk-harvest org-related content.
