# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.003 - Firmware
id: 8549320c-b463-5386-8ee8-12e9b00154c4
status: experimental
description: 'SNMP bulk walk - full MIB firmware and configuration harvest NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1592.003
    - CISA ICS-CERT advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.003
    - attack.g0035
    - attack.g0045
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port: 161
        network.transport: udp
        destination.bytes|gt: 200
        network.packets|gt: 10
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 161
        AND network.transport: udp
        AND destination.bytes > 200
        AND network.packets > 10
    arkime: |-
        ip.src != $MPNET
        && port.dst == 161
        && protocols == udp
        && databytes.src > 200
        && packets.src > 10
    suricata: |-
        alert udp $EXTERNAL_NET any
          -> $HOME_NET 161
          (msg:"RECON T1592.003 SNMP bulk
            MIB walk firmware harvest";
          content:"|30|"; depth:1;
          content:"|a5|"; offset:5;
          depth:1;
          threshold:type both,
            track by_src,
            count 10, seconds 30;
          classtype:attempted-recon;
          sid:9159214; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (destination.bytes > 200): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (network.packets > 10): emitted as |gt modifier, backend support varies'
    actors:
        - Dragonfly
        - APT10
        - Multi
    analyst_notes: SNMP GetBulkRequest (PDU type 0xa5) retrieves multiple OIDs in a single request. A full MIB walk returns sysDescr, interfaces, ARP tables, routing tables, CDP neighbor data, and proprietary MIBs containing running config hashes and boot image names - a complete device profile. High packet count + large payload from external IP to UDP/161 = active bulk walk.
