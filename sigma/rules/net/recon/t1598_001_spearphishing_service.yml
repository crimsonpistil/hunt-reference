# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.001 - Spearphishing Service
id: 3584dccc-79fd-5dcd-86e6-3172b81a7a04
status: experimental
description: Inbound phishing infrastructure - newly registered MX hitting mail gateway
references:
    - MITRE ATT&CK T1598
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.001
    - attack.g0059
    - attack.g0094
    - attack.g0016
    - attack.g1036
logsource:
    category: network_connection
    product: zeek
    service: ssl
detection:
    selection:
        destination.ip: $MAIL_SERVERS
        destination.port:
            - 25
            - 587
        tls.server.not_before: '[now-30d TO now]'
    filter_0:
        source.ip: $ALLOWED_MX
    filter_1:
        source.ip: $ALLOWED_DEFAULTS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        destination.ip: $MAIL_SERVERS
        AND destination.port: (25 OR 587)
        AND NOT source.ip: $ALLOWED_MX
        AND NOT source.ip: $ALLOWED_DEFAULTS
        AND tls.server.not_before:
          [now-30d TO now]
    arkime: |-
        ip.dst == $MAIL_SERVERS
        && port.dst == [25, 587]
        && protocols == smtp
        && ip.src != $ALLOWED_MX
        && ip.src != $ALLOWED_DEFAULTS
    suricata: |-
        alert smtp $EXTERNAL_NET any
          -> $SMTP_SERVERS [25,587]
          (msg:"RECON T1598 Inbound SMTP
            unknown/new sending IP";
          flow:established,to_server;
          content:"EHLO"; nocase;
          threshold:type both,
            track by_src, count 3,
            seconds 60;
          classtype:social-engineering;
          sid:9159807; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $ALLOWED_MX
        - $MAIL_SERVERS
    actors:
        - Charming Kitten
        - Kimsuky
        - APT29
        - Moonstone Sleet
    analyst_notes: Pair with SPF/DKIM/DMARC fail tags. New sending IP + DMARC fail + auth-themed subject = near-certain spearphish. Enrich against GreyNoise, Spamhaus, AbuseIPDB. Cross-reference vendor email allowlist - legitimate vendors do sometimes send from new IPs.
