# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1593.001 - Social Media
id: a90a35ce-30d2-5fa5-810b-9829ebb0c0b1
status: experimental
description: 'LinkedIn bulk profile / company scraping from internal host NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1593.001
    - T1589.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1593
    - attack.t1593.001
    - attack.g0059
    - attack.g0094
    - attack.g0045
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain: linkedin.com
        http.request.method: GET
        url.path:
            - '*search/results/people*'
            - '*/company/*'
            - '*/in/*'
            - '*/posts/*'
        http.response.bytes|gt: 20000
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1593
    technique_name: Search Open Websites / Domains
    original_query: |-
        source.ip: $MPNET
        AND url.domain: "linkedin.com"
        AND http.request.method: GET
        AND url.path: (
          *search/results/people*
          OR */company/*
          OR */in/*
          OR */posts/*
        )
        AND http.response.bytes > 20000
    arkime: |-
        ip.src == $MPNET
        && host.http == *linkedin.com*
        && http.method == GET
        && http.uri == ["*/search/results/people*", "*/company/*", "*/in/*", "*/posts/*"]
        && databytes.src > 20000
        && packets.src > 20
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1593.001 Internal
            LinkedIn bulk profile scrape";
          flow:established,to_server;
          content:"GET"; http.method;
          content:"linkedin.com"; host.http;
          pcre:"/(search\/results\/people|
            \/company\/|
            \/in\/[a-z0-9\-]+\/)/ix";
          http.uri;
          threshold:type both,
            track by_src,
            count 20, seconds 60;
          classtype:policy-violation;
          sid:9159301; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (http.response.bytes > 20000): emitted as |gt modifier, backend support varies'
    actors:
        - Charming Kitten
        - Kimsuky
        - APT10
    analyst_notes: High-volume LinkedIn profile and company page fetches from single internal host = automated scraping. Legitimate LinkedIn use is interactive - 20+ profile GETs in 60 seconds from one endpoint is anomalous. LinkedIn rate limiting generates 429 responses visible in proxy logs - a 429 from LinkedIn is itself an indicator. Baseline sales/recruiting team use first.
