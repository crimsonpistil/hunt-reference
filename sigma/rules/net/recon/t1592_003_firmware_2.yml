# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.003 - Firmware
id: 8549320c-b463-5386-8ee8-12e9b00154c4
status: experimental
description: Network device HTTP management interface - firmware version page probing
references:
    - MITRE ATT&CK T1592.003
    - CISA AA23-144A
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.003
    - attack.g1017
    - attack.g0064
    - attack.g0034
logsource:
    category: proxy
detection:
    selection:
        http.request.method: GET
        url.path:
            - '*firmware*'
            - '*version*'
            - '*/cgi-bin/luci*'
            - '*/webui/login*'
            - '*/admin/status.php*'
            - '*/system/device-info*'
            - '*/api/v1/system/info*'
            - '*/rest/system/info*'
    filter_0:
        source.ip: $MPNET
    filter_1:
        url.domain: $ALLOWED_DEFAULTS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: GET
        AND url.path: (
          *firmware* OR *version*
          OR */cgi-bin/luci*
          OR */webui/login*
          OR */admin/status.php*
          OR */system/device-info*
          OR */api/v1/system/info*
          OR */rest/system/info*
        )
        AND NOT url.domain: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src != $MPNET
        && protocols == http
        && http.method == GET
        && http.uri == ["*/firmware*", "*/version*", "*/cgi-bin/luci*", "*/webui/login*", "*/admin/status.php*", "*/system/device-info*", "*/api/v1/system/info*", "*/rest/system/info*"]
        && host.http != $ALLOWED_DEFAULTS
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1592.003 Device mgmt
            firmware version page probe";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/(firmware|version|
            cgi-bin\/luci|webui\/login|
            admin\/status\.php|
            system\/device-info|
            api\/v1\/system\/info|
            rest\/system\/info)/ix";
          http.uri;
          classtype:attempted-recon;
          sid:9159215; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - Volt Typhoon
        - APT33
        - Sandworm
    analyst_notes: 'Network devices expose firmware version information on management web interface pages - often without authentication on the version/status/about page. Path signatures: /cgi-bin/luci = OpenWRT/LEDE, /webui/login = UTM appliances, /api/v1/system/info = Ubiquiti/Aruba/Ruckus. A 200 response disclosing a firmware version = immediate patch status check. Cross-reference response body against CVE database.'
