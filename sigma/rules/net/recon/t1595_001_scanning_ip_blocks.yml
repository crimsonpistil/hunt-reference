# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1595.001 - Scanning IP Blocks
id: 2ef8d773-fc13-5794-92f0-6397fec69208
status: experimental
description: SYN scan pattern - single packet each direction, no data exchange
references:
    - MITRE ATT&CK T1595.001
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1595
    - attack.t1595.001
    - attack.g0065
    - attack.g0007
    - attack.g0034
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        tcp.flags: S
        network.packets: 2
        source.bytes: 0
        destination.bytes: 0
    filter_0:
        source.ip: $MPNET
    filter_1:
        tcp.flags: A
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - High count threshold avoids false positives on slow apps
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1595
    technique_name: Active Scanning
    original_query: |-
        NOT source.ip: $MPNET
        AND tcp.flags: "S"
        AND NOT tcp.flags: "A"
        AND network.packets: 2
        AND source.bytes: 0
        AND destination.bytes: 0
    arkime: |-
        ip.src != $MPNET
        && packets.src == 1
        && packets.dst == 1
        && databytes.src == 0
        && databytes.dst == 0
        && tcpflags.syn == 1
        && tcpflags.ack == 0
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"RECON T1595 SYN scan";
          flags:S,12; flow:stateless;
          threshold:type both,
            track by_src,
            count 20, seconds 10;
          classtype:attempted-recon;
          sid:9159501; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT40
        - APT28
        - Sandworm
        - Multi/IAB
    analyst_notes: flags:S,12 matches SYN only, ignoring RST/ACK/FIN. Exactly 1 pkt each direction with 0 databytes is the SYN→SYN-ACK→RST fingerprint. High count threshold avoids false positives on slow apps.
