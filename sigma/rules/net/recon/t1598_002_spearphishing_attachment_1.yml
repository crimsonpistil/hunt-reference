# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.002 - Spearphishing Attachment
id: 525398c5-5050-5c11-922e-cbf2bf812620
status: experimental
description: Remote template fetch - post-email open outbound connection
references:
    - MITRE ATT&CK T1598.002
    - T1221
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.002
    - attack.g0065
    - attack.g0096
    - attack.g0007
    - attack.g0059
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.path:
            - '*.dotx*'
            - '*.dot*'
            - '*.xltx*'
            - '*.potx*'
            - '*.sct*'
            - '*.hta*'
            - '*.wsdl*'
        http.request.referrer:
            - '*outlook*'
            - '*mail*'
            - '*webmail*'
            - '*owa*'
    filter_0:
        url.domain: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        source.ip: $MPNET
        AND NOT url.domain: $ALLOWED_DEFAULTS
        AND url.path: (
          *.dotx* OR *.dot*
          OR *.xltx* OR *.potx*
          OR *.sct* OR *.hta*
          OR *.wsdl*
        )
        AND http.request.referrer: (
          *outlook* OR *mail*
          OR *webmail* OR *owa*
        )
    arkime: |-
        ip.src == $MPNET
        && protocols == http
        && http.uri == ["*.dotx*", "*.dot*", "*.xltx*", "*.potx*", "*.sct*", "*.hta*", "*.wsdl*"]
        && host.http != $ALLOWED_DEFAULTS
        && http.hasheader.src.value == ["*outlook*", "*mail*", "*webmail*", "*owa*"]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1598.002 Remote
            template fetch post-email";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/\.(dotx?|xltx|potx|
            sct|hta|wsdl)(\?|$)/i";
          http.uri;
          classtype:social-engineering;
          sid:9159806; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - APT40
        - APT41
        - APT28
        - Charming Kitten
    analyst_notes: Clean document fetches malicious template on open (T1221). Outlook/OWA referrer + .dotx from unknown external host is strong correlation. UNC path triggers (SMB to external IP) won't appear in HTTP logs - catch in Zeek conn.log. The document itself evades AV; the network fetch is the only detection opportunity.
