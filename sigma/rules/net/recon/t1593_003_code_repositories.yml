# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1593.003 - Code Repositories
id: 5196be74-0d0d-5630-a6ae-d302e07e1ed1
status: experimental
description: GitHub API search for org secrets / internal naming from internal host
references:
    - MITRE ATT&CK T1593.003
    - CISA advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1593
    - attack.t1593.003
    - attack.g0096
    - attack.g0016
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        url.domain:
            - github.com
            - api.github.com
        url.path:
            - '*search/code*'
            - '*search/repositories*'
        url.query:
            - '*<YOUR_DOMAIN>*'
            - '*internal*'
            - '*password*'
            - '*secret*'
            - '*api_key*'
            - '*token*'
            - '*BEGIN+RSA*'
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1593
    technique_name: Search Open Websites / Domains
    original_query: |-
        source.ip: $MPNET
        AND url.domain: (
          "github.com"
          OR "api.github.com"
        )
        AND url.path: (
          *search/code*
          OR *search/repositories*
        )
        AND url.query: (
          *<YOUR_DOMAIN>* OR *internal*
          OR *password* OR *secret*
          OR *api_key* OR *token*
          OR *BEGIN+RSA*
        )
    arkime: |-
        ip.src == $MPNET
        && host.http == ["*github.com*", "*api.github.com*"]
        && http.uri == ["*/search/code*", "*/search?q=*"]
        && http.uri == ["*password*", "*secret*", "*api_key*", "*token*", "*<YOUR_DOMAIN>*", "*internal*"]
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1593.003 Internal
            GitHub code search secret
            exposure hunting";
          flow:established,to_server;
          content:"api.github.com"; host.http;
          content:"/search/code"; http.uri;
          pcre:"/(password|secret|
            api_key|token|BEGIN.RSA|
            BEGIN.PGP|internal|corp)/ix";
          http.uri;
          classtype:policy-violation;
          sid:9159305; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - Cozy Bear
        - APT33
    analyst_notes: GitHub code search API queries targeting your org's domain, internal naming conventions, or credential keywords from internal hosts = security team secret scanning (document it) or compromised host hunting for accidentally committed credentials. GitHub API rate-limits at 10 req/min for authenticated users - 403 responses in proxy logs indicate automated tooling hitting rate limits.
