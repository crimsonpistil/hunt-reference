# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.002 - Spearphishing Attachment
id: 525398c5-5050-5c11-922e-cbf2bf812620
status: experimental
description: DNS OOB callback - encoded subdomain from phishing document
references:
    - MITRE ATT&CK T1598.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.002
    - attack.g0065
    - attack.g0007
    - attack.g0059
    - attack.g0094
logsource:
    category: dns
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        dns.question.type:
            - A
            - AAAA
            - TXT
        dns.question.name|re: |-

              [0-9a-f]{8,}\.|
              [A-Za-z0-9]{20,}\.
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        source.ip: $MPNET
        AND dns.question.type: (
          "A" OR "AAAA" OR "TXT"
        )
        AND dns.question.name: /
          [0-9a-f]{8,}\.|
          [A-Za-z0-9]{20,}\.
        /
    arkime: |-
        ip.src == $MPNET
        && protocols == dns
        && dns.query.type == [A, AAAA, TXT]
        && host.dns != $ALLOWED_DEFAULTS
        // Encoded-subdomain detection (long hex or base64 strings) requires regex - does not always play nice in Arkime. 
        // See Suricata pcre column or use Kibana KQLegex syntax for runtime matching.

        // Logical spec: host.dns matches
        //   /[0-9a-f]{8,}\./ or /[A-Za-z0-9+]{16,}\./
    suricata: |-
        alert dns $HOME_NET any
          -> any any
          (msg:"RECON T1598 OOB DNS
            encoded subdomain callback";
          dns.query;
          pcre:"/^([0-9a-f]{8,}|
            [A-Za-z0-9+\/]{16,})\./i";
          classtype:attempted-recon;
          sid:9159805; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT40
        - APT28
        - APT35
        - Kimsuky
    analyst_notes: DOCX remote templates, OLE links, XXE, SVG/CSS imports fire DNS lookups encoding victim hostname/user/IP in the subdomain. TXT queries used for data exfil. Flag any internal host resolving a subdomain with 8+ consecutive hex chars or 16+ base64 chars.
