# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.003 - Spearphishing Link
id: 319a68c9-b4f8-51a1-9325-a1cddb4e3683
status: experimental
description: AiTM / Evilginx proxy - session cookie harvest post-MFA
references:
    - MITRE ATT&CK T1598
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.003
    - attack.g0016
    - attack.g0059
    - attack.g1015
logsource:
    category: network_connection
    product: zeek
    service: ssl
detection:
    selection:
        source.ip: $MPNET
        http.response.headers.set_cookie: '*'
        tls.server.not_before: '[now-14d TO now]'
    filter_0:
        tls.server.name: $ALLOWED_DEFAULTS
    filter_1:
        url.domain: $ALLOWED_DEFAULTS
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        source.ip: $MPNET
        AND NOT tls.server.name: $ALLOWED_DEFAULTS
        AND http.response.headers.set_cookie: *
        AND tls.server.not_before:
          [now-14d TO now]
        AND NOT url.domain: $ALLOWED_DEFAULTS
    arkime: |-
        ip.src == $MPNET
        && protocols == https
        && host.http != $ALLOWED_DEFAULTS
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"RECON T1598 AiTM proxy
            Set-Cookie unknown domain";
          flow:established,from_server;
          content:"Set-Cookie:"; http.header;
          content:"Secure"; http.header;
          content:"HttpOnly"; http.header;
          threshold:type both,
            track by_dst, count 2,
            seconds 10;
          classtype:social-engineering;
          sid:9159803; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    actors:
        - Midnight Blizzard
        - Charming Kitten
        - Scattered Spider
    analyst_notes: 'AiTM frameworks (Evilginx2, Modlishka, Muraena) proxy the real IdP - MFA succeeds but session token is captured. Look for Secure+HttpOnly cookies set by domains NOT in your IdP list (Okta, Azure AD, Duo). Follow up in identity logs: successful MFA + new device or impossible travel = confirmed incident.'
