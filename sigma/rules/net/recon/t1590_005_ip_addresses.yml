# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1590.005 - IP Addresses
id: 566fa0d3-423a-5482-8474-cc81ebdca871
status: experimental
description: Reverse DNS / PTR walking of your IP ranges
references:
    - MITRE ATT&CK T1590.005
    - CISA AA22-076A
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1590
    - attack.t1590.005
    - attack.g0045
    - attack.g0034
logsource:
    category: dns
    product: zeek
detection:
    selection:
        dns.question.type: PTR
        dns.question.name: '*.in-addr.arpa'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1590
    technique_name: Gather Victim Network Information
    original_query: |-
        NOT source.ip: $MPNET
        AND dns.question.type: "PTR"
        AND dns.question.name:
          *.in-addr.arpa
    arkime: |-
        ip.src != $MPNET
        && protocols == dns
        && dns.query.type == PTR
        && host.dns == *.in-addr.arpa
        && packets.src > 30
    suricata: |-
        alert dns $EXTERNAL_NET any
          -> $DNS_SERVERS any
          (msg:"RECON T1590.005 PTR sweep
            reverse DNS enumeration";
          dns.query;
          content:".in-addr.arpa";
          threshold:type both,
            track by_src,
            count 15, seconds 20;
          classtype:attempted-recon;
          sid:9159003; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT10
        - Sandworm
        - Multi
    analyst_notes: Sequential PTR queries map hostnames to IPs without touching hosts directly. Confirm sequential last-octet increments in Arkime to distinguish from legitimate resolver behavior. Walking a /24 in under 60 seconds with incrementing octets = near-certain automated enumeration.
