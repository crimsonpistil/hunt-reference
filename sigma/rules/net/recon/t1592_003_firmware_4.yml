# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.003 - Firmware
id: 8549320c-b463-5386-8ee8-12e9b00154c4
status: experimental
description: TLS certificate CN / SAN - device firmware version and model disclosure
references:
    - MITRE ATT&CK T1592.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.003
    - attack.g0064
    - attack.g1017
logsource:
    category: network_connection
    product: zeek
    service: ssl
detection:
    selection:
        tls.server.x509.subject.common_name:
            - '*FortiGate*'
            - '*SonicWall*'
            - '*pfSense*'
            - '*OPNsense*'
            - '*Cisco*'
            - '*Juniper*'
            - '*Ubiquiti*'
            - '*MikroTik*'
            - '*Synology*'
            - '*QNAP*'
            - '*router*'
            - '*firewall*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND tls.server.x509.subject.common_name: (
          *FortiGate* OR *SonicWall*
          OR *pfSense* OR *OPNsense*
          OR *Cisco* OR *Juniper*
          OR *Ubiquiti* OR *MikroTik*
          OR *Synology* OR *QNAP*
          OR *router* OR *firewall*
        )
    arkime: |-
        ip.src != $MPNET
        && protocols == tls
        && cert.subject.cn == ["*FortiGate*", "*SonicWall*", "*pfSense*", "*OPNsense*", "*Cisco*", "*Juniper*", "*Ubiquiti*", "*MikroTik*", "*Synology*", "*QNAP*", "*router*", "*firewall*", "*ESXi*", "*vCenter*", "*iLO*", "*iDRAC*"]
    suricata: |-
        alert tls $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1592.003 TLS cert
            device model/firmware disclose";
          flow:established,from_server;
          tls.cert_subject;
          pcre:"/CN=[^,]*(FortiGate|
            SonicWall|pfSense|OPNsense|
            Cisco|Juniper|Ubiquiti|
            MikroTik|Synology|QNAP|
            router|firewall)/ix";
          classtype:policy-violation;
          sid:9159217; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT33
        - Volt Typhoon
        - Multi
    analyst_notes: 'Network devices generating self-signed TLS certs often include device model, hostname, and sometimes firmware version in the CN or SAN fields. A FortiGate self-signed cert with CN=FortiGate-100F reveals exact hardware model - adversaries correlate with Shodan to track firmware update history. Remediate: replace self-signed certs with CA-issued certs with generic CNs.'
