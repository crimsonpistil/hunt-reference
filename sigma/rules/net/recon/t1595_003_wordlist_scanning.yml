# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1595.003 - Wordlist Scanning
id: 897557df-9120-5bb4-b274-59a28d74b85a
status: experimental
description: High 4xx response ratio from single external IP - wordlist exhaustion
references:
    - MITRE ATT&CK T1595.003
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1595
    - attack.t1595.003
    - attack.g0096
    - attack.g0064
    - attack.g0046
logsource:
    category: proxy
detection:
    selection:
        http.response.status_code:
            - |-
                (
                  400
            - 403
            - 404
            - |-
                405
                )
                | stats count by source.ip
                | where count > 50
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1595
    technique_name: Active Scanning
    original_query: |-
        NOT source.ip: $MPNET
        AND http.response.status_code: (
          400 OR 403 OR 404 OR 405
        )
        | stats count by source.ip
        | where count > 50
    arkime: |-
        ip.src != $MPNET
        && protocols == http
        && http.statuscode == [400, 403, 404, 405]
        && packets.src > 50
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1595.003 High 4xx
            ratio wordlist exhaustion";
          flow:established,to_server;
          content:"GET"; http.method;
          http.stat_code;
          content:"404";
          threshold:type both,
            track by_src,
            count 50, seconds 60;
          classtype:web-application-activity;
          sid:9159508; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - APT33
        - FIN7
    analyst_notes: Wordlist scanners (gobuster, ffuf, feroxbuster) generate characteristic 404/403/400 bursts regardless of UA string. Single external IP generating 50+ 4xx responses across varied URI paths in 60 seconds = near-certain wordlist scanning. UA-spoof-resistant companion to the scanner UA row. Use bucket aggregation in Kibana (source.ip + status_code) - signal is in the volume, not individual requests.
