# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.001 - Hardware
id: 7f3261dc-0d17-5226-bb59-51b709af5114
status: experimental
description: UPnP description XML fetch - hardware detail harvest post-SSDP discovery
references:
    - MITRE ATT&CK T1592.001
    - CISA AA23-144A
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.001
    - attack.g1017
    - attack.g0034
logsource:
    category: proxy
detection:
    selection:
        http.request.method: GET
        url.path:
            - '*rootDesc.xml*'
            - '*upnp/desc*'
            - '*device.xml*'
            - '*DeviceDescription*'
            - '*igd.xml*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: GET
        AND url.path: (
          *rootDesc.xml*
          OR *upnp/desc*
          OR *device.xml*
          OR *DeviceDescription*
          OR *igd.xml*
        )
    arkime: |-
        ip.src != $MPNET
        && protocols == http
        && http.method == GET
        && http.uri == ["*/rootDesc.xml*", "*/upnp/desc*", "*/device.xml*", "*/DeviceDescription*", "*/igd.xml*"]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HOME_NET any
          (msg:"RECON T1592.001 UPnP
            device description XML fetch";
          flow:established,to_server;
          content:"GET"; http.method;
          pcre:"/(rootDesc\.xml|
            upnp\/desc|device\.xml|
            DeviceDescription|
            igd\.xml)/ix";
          http.uri;
          classtype:attempted-recon;
          sid:9159204; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Volt Typhoon
        - Sandworm
        - Multi
    analyst_notes: 'After SSDP discovery, adversaries fetch the UPnP device description XML containing the full hardware profile: manufacturer, model number, serial number, firmware version, and UPnP services. Two-step chain: SSDP discovers what''s present, description XML retrieves details. A 200 response to rootDesc.xml from an external IP is both a misconfiguration and an active hardware disclosure event.'
