# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1589.002 - Email Addresses
id: 0ec1c002-0ff1-52fe-88d8-73acf7798628
status: experimental
description: 'SMTP RCPT TO oracle - valid vs invalid address discrimination NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1589.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1589
    - attack.t1589.002
    - attack.g0059
    - attack.g0094
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port: 25
        network.packets|gt: 40
    filter_0:
        source.ip: $MPNET
    filter_1:
        source.ip: $ALLOWED_MX
    condition: selection and not filter_0 and not filter_1
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1589
    technique_name: Gather Victim Identity Information
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: 25
        AND NOT source.ip: $ALLOWED_MX
        AND network.packets > 40
    arkime: |-
        ip.src != $MPNET
        && port.dst == 25
        && protocols == smtp
        && ip.src != $ALLOWED_MX
        && packets.src > 20
        && packets.dst > 20
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $SMTP_SERVERS 25
          (msg:"RECON T1589.002 SMTP
            RCPT TO oracle enum";
          flow:established,to_server;
          content:"RCPT TO:"; nocase;
          threshold:type both,
            track by_src, count 10,
            seconds 60;
          classtype:attempted-recon;
          sid:9158906; rev:1;)
    requires_variables:
        - $ALLOWED_MX
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (network.packets > 40): emitted as |gt modifier, backend support varies'
    actors:
        - APT35
        - Kimsuky
        - Multi
    analyst_notes: 'Even with VRFY/EXPN disabled - different response codes for valid/invalid RCPT TO = oracle. High packet count with many RCPT TO lines in SMTP stream is the indicator. Tools: smtp-user-enum, Metasploit smtp_enum, swaks. Catch-all configuration eliminates this entirely.'
