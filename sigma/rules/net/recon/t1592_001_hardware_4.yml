# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.001 - Hardware
id: 7f3261dc-0d17-5226-bb59-51b709af5114
status: experimental
description: 'WMI remote queries - external or lateral WMI hardware enumeration (DCOM/RPC) NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1592.001
    - T1047
    - CISA advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.001
    - attack.g0096
    - attack.g0007
    - attack.g0032
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        destination.port:
            - 135
            - 445
        network.transport: tcp
        source.bytes|gt: 0
        destination.bytes|gt: 0
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND destination.port: (135 OR 445)
        AND network.transport: tcp
        AND source.bytes > 0
        AND destination.bytes > 0
    arkime: |-
        ip.src != $MPNET
        && port.dst == [135, 445]
        && protocols == [dce-rpc, smb]
        && databytes.src > 0
        && databytes.dst > 0
    suricata: |-
        alert tcp $EXTERNAL_NET any
          -> $HOME_NET [135,445]
          (msg:"RECON T1592.001 External
            WMI/DCOM hardware enum";
          flow:established,to_server;
          content:"|05 00|"; depth:2;
          threshold:type both,
            track by_src,
            count 3, seconds 30;
          classtype:attempted-recon;
          sid:9159205; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 0): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (destination.bytes > 0): emitted as |gt modifier, backend support varies'
    actors:
        - APT41
        - APT28
        - Lazarus
    analyst_notes: WMI over DCOM (TCP/135 + dynamic high ports) allows remote hardware enumeration - Win32_ComputerSystem returns manufacturer/model/memory. External TCP/135 or TCP/445 should never reach internal hosts. Content '|05 00|' matches DCE/RPC bind header. Zeek dce_rpc.log captures operation names - look for IWbemServices::ExecQuery calls.
