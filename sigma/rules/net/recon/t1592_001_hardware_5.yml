# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.001 - Hardware
id: 7f3261dc-0d17-5226-bb59-51b709af5114
status: experimental
description: MAC address OUI harvesting via ARP sweep - hardware vendor identification
references:
    - MITRE ATT&CK T1592.001
    - T1018
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.001
    - attack.g0034
    - attack.g0096
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        network.type: ipv4
        network.transport: arp
    condition: selection
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        source.ip: $MPNET
        AND network.type: "ipv4"
        AND network.transport: "arp"
    arkime: |-
        ip.src == $MPNET
        && protocols == arp
        && packets.src > 30
    suricata: |-
        alert arp $HOME_NET any
          -> any any
          (msg:"RECON T1592.001 ARP sweep
            MAC/OUI hardware enumeration";
          content:"|00 01|"; offset:6;
          depth:2;
          threshold:type both,
            track by_src,
            count 30, seconds 30;
          classtype:attempted-recon;
          sid:9159206; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Sandworm
        - APT41
        - Multi
    analyst_notes: ARP responses include the MAC address of each responding host. The first three octets (OUI) identify the hardware vendor - Cisco, Dell, Fortinet, Raspberry Pi, VMware. An internal host generating 30+ ARP requests in 30 seconds is performing a sweep. Requires existing foothold (ARP is layer 2). Zeek arp.log captures all ARP activity including OUIs.
