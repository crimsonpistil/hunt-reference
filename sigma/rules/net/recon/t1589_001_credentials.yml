# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1589.001 - Credentials
id: 718ffd84-ba64-52b5-9dd1-ff1ea90a53ca
status: experimental
description: Azure AD / O365 GetCredentialType username enumeration
references:
    - MITRE ATT&CK T1589.001
    - Microsoft MSTIC
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1589
    - attack.t1589.001
    - attack.g0016
    - attack.g0059
    - attack.g0094
    - attack.g0128
logsource:
    category: proxy
detection:
    selection:
        url.domain: login.microsoftonline.com
        url.path: '*GetCredentialType*'
        http.request.method: POST
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1589
    technique_name: Gather Victim Identity Information
    original_query: |-
        NOT source.ip: $MPNET
        AND url.domain:
          "login.microsoftonline.com"
        AND url.path: *GetCredentialType*
        AND http.request.method: POST
    arkime: |-
        ip.src != $MPNET
        && http.method == POST
        && host.http ==
          *login.microsoftonline.com*
        && http.uri ==
          */GetCredentialType*
        && packets.src > 5
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1589.001 AzureAD
            GetCredentialType user enum";
          flow:established,to_server;
          content:"POST"; http.method;
          content:"/common/GetCredentialType";
          http.uri;
          threshold:type both,
            track by_src,
            count 10, seconds 60;
          classtype:attempted-recon;
          sid:9158901; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Midnight Blizzard
        - Charming Kitten
        - Kimsuky
        - ZIRCONIUM
    analyst_notes: 'Returns UPN existence, auth method, and federation status without authentication. Tools: AADInternals, o365enum, TREVORspray. Enumerate thousands of usernames via this endpoint. Monitor via proxy/CASB egress logs - this hits Microsoft infrastructure, not your perimeter directly.'
