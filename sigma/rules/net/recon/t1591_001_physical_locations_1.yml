# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1591.001 - Physical Locations
id: ab9eec47-4d73-553e-bb87-c0f27cd4b2c3
status: experimental
description: Bulk subsidiary / affiliate DNS enumeration
references:
    - MITRE ATT&CK T1591
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1591
    - attack.t1591.001
    - attack.g0096
    - attack.g1017
    - attack.g0034
logsource:
    category: dns
    product: zeek
detection:
    selection:
        dns.question.type: A
        dns.question.name:
            - '*corp*'
            - '*internal*'
            - '*dev*'
            - '*staging*'
            - '*subsidiary*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1591
    technique_name: Gather Victim Org Information
    original_query: |-
        NOT source.ip: $MPNET
        AND dns.question.type: "A"
        AND dns.question.name: (
          *corp* OR *internal*
          OR *dev* OR *staging*
          OR *subsidiary*
        )
    arkime: |-
        ip.src != $MPNET
        && protocols == dns
        && dns.query.type == A
        && host.dns == *.corp.*
        && packets.src > 50
    suricata: |-
        alert dns $EXTERNAL_NET any
          -> $DNS_SERVERS any
          (msg:"RECON T1591 Subsidiary
            DNS bulk enum";
          dns.query;
          content:".corp.";
          threshold:type both,
            track by_src,
            count 15, seconds 30;
          classtype:attempted-recon;
          sid:9159102; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT41
        - Volt Typhoon
        - Sandworm
    analyst_notes: External IPs resolving many internal subdomain patterns suggests org structure mapping. Pair with passive DNS to identify which names actually resolved vs NXDOMAIN flood. Watch NXDOMAIN ratio across all sources combined for slow-and-low variants.
