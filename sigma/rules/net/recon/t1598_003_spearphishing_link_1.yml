# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.003 - Spearphishing Link
id: 319a68c9-b4f8-51a1-9325-a1cddb4e3683
status: experimental
description: 'Internal host POSTing credentials to external harvester page NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1598
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.003
    - attack.g0064
    - attack.g0059
    - attack.g0016
    - attack.g0094
logsource:
    category: proxy
detection:
    selection:
        source.ip: $MPNET
        http.request.method: POST
        url.path:
            - '*login*'
            - '*signin*'
            - '*verify*'
            - '*auth*'
            - '*account*'
            - '*password*'
        http.request.body.bytes|gt: 50
        http.request.body.bytes|lt: 500
    filter_0:
        url.domain: $ALLOWED_DEFAULTS
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: high
tonk:
    fidelity: partial
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        source.ip: $MPNET
        AND http.request.method: POST
        AND NOT url.domain: $ALLOWED_DEFAULTS
        AND url.path: (
          *login* OR *signin*
          OR *verify* OR *auth*
          OR *account* OR *password*
        )
        AND http.request.body.bytes > 50
        AND http.request.body.bytes < 500
    arkime: |-
        ip.src == $MPNET
        && http.method == POST
        && host.http != $ALLOWED_DEFAULTS
        && http.uri == ["*login*", "*signin*", "*verify*", "*auth*", "*password*", "*credential*"]
        && databytes.src > 50
        && databytes.src < 500
    suricata: |-
        alert http $HOME_NET any
          -> $EXTERNAL_NET any
          (msg:"RECON T1598 Cred POST
            to external URI";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(login|signin|verify|
            auth|account|confirm|
            password|credential)/i";
          http.uri;
          content:"password=";
          http.request_body;
          classtype:social-engineering;
          sid:9159802; rev:1;)
    requires_variables:
        - $ALLOWED_DEFAULTS
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (http.request.body.bytes > 50): emitted as |gt modifier, backend support varies'
        - 'numeric-comparison (http.request.body.bytes < 500): emitted as |lt modifier, backend support varies'
    actors:
        - APT33
        - Charming Kitten
        - APT29
        - Kimsuky
    analyst_notes: POST body 50-500 bytes is the sweet spot for a username+password submission. Also match 'passwd=', 'pwd=', 'pass=', 'credential=' variants. Pair with proxy category for the destination domain.
