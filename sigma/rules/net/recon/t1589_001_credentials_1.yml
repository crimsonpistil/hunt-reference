# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1589.001 - Credentials
id: 718ffd84-ba64-52b5-9dd1-ff1ea90a53ca
status: experimental
description: O365 Autodiscover username validation
references:
    - MITRE ATT&CK T1589.001
    - CISA Exchange advisories
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1589
    - attack.t1589.001
    - attack.g0045
    - attack.g0007
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        url.domain:
            - '*autodiscover*'
            - outlook.office365.com
        url.path:
            - '*autodiscover.xml*'
            - '*autodiscover.json*'
            - '*mapi/emsmdb*'
            - '*mapi/nspi*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1589
    technique_name: Gather Victim Identity Information
    original_query: |-
        NOT source.ip: $MPNET
        AND url.domain: (
          *autodiscover*
          OR "outlook.office365.com"
        )
        AND url.path: (
          *autodiscover.xml*
          OR *autodiscover.json*
          OR *mapi/emsmdb*
          OR *mapi/nspi*
        )
    arkime: |-
        ip.src != $MPNET
        && http.method == [GET, POST]
        && host.http == ["*autodiscover*", "*outlook.office365.com*"]
        && http.uri == ["*/autodiscover.xml*", "*/autodiscover.json*", "*/mapi/emsmdb*", "*/mapi/nspi*"]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1589.001
            Autodiscover user enum";
          flow:established,to_server;
          pcre:"/(autodiscover\.xml|
            autodiscover\.json|
            mapi\/emsmdb|
            mapi\/nspi)/ix";
          http.uri;
          threshold:type both,
            track by_src, count 5,
            seconds 30;
          classtype:attempted-recon;
          sid:9158902; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT10
        - APT28
        - APT33
    analyst_notes: 'Autodiscover returns different HTTP response codes (200 vs 401 vs 404) per username - classic oracle. MAPI/NSPI endpoints particularly abused for Outlook profile enumeration. Tools: MailSniper, ruler, o365recon. 200 response to autodiscover with no prior auth session is a strong signal.'
