# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1598.003 - Spearphishing Link
id: 319a68c9-b4f8-51a1-9325-a1cddb4e3683
status: experimental
description: MFA token harvesting - rapid sequential TOTP/OTP submission
references:
    - MITRE ATT&CK T1598
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1598
    - attack.t1598.003
    - attack.g1015
    - attack.g0016
    - attack.g0032
    - attack.g0094
logsource:
    category: proxy
detection:
    selection:
        http.request.method: POST
        url.path:
            - '*mfa*'
            - '*otp*'
            - '*totp*'
            - '*2fa*'
            - '*verify*'
            - '*code*'
            - '*challenge*'
        http.response.status_code:
            - 200
            - 302
            - 401
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1598
    technique_name: Phishing for Information
    original_query: |-
        NOT source.ip: $MPNET
        AND http.request.method: POST
        AND url.path: (
          *mfa* OR *otp* OR *totp*
          OR *2fa* OR *verify*
          OR *code* OR *challenge*
        )
        AND http.response.status_code:
          (200 OR 302 OR 401)
    arkime: |-
        ip.src != $MPNET
        && http.method == POST
        && http.uri == ["*mfa*", "*otp*", "*totp*", "*2fa*", "*verify*", "*code*", "*challenge*"]
        && packets.src > 5
        && http.statuscode == [200, 302, 401]
    suricata: |-
        alert http $EXTERNAL_NET any
          -> $HTTP_SERVERS any
          (msg:"RECON T1598 MFA OTP
            brute/harvest attempt";
          flow:established,to_server;
          content:"POST"; http.method;
          pcre:"/(mfa|otp|totp|2fa|
            verify|challenge|token)/i";
          http.uri;
          threshold:type both,
            track by_src, count 5,
            seconds 30;
          classtype:attempted-user;
          sid:9159804; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - Scattered Spider
        - Midnight Blizzard
        - Lazarus
        - Kimsuky
    analyst_notes: 'Real-time kits (EvilProxy) relay OTP within the 30s TOTP window - creates a burst of POSTs to /mfa or /verify endpoints. Also watch for MFA fatigue: repeated push notifications to the same account in short window - visible in IdP logs, not network traffic.'
