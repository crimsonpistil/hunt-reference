# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/recon.js
title: T1592.002 - Software
id: ce4e8096-fc6c-58e7-aa05-60cea3161b37
status: experimental
description: HTTP Server header software version disclosure in egress responses
references:
    - MITRE ATT&CK T1592.002
    - industry reporting
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.reconnaissance
    - attack.t1592
    - attack.t1592.002
    - attack.g0065
    - attack.g0064
logsource:
    category: proxy
detection:
    selection:
        http.response.status_code:
            - 200
            - 301
            - 302
            - 400
            - 403
            - 404
        http.response.headers.server:
            - '*Apache*'
            - '*nginx*'
            - '*Microsoft-IIS*'
            - '*PHP*'
            - '*Tomcat*'
            - '*Jetty*'
            - '*Werkzeug*'
            - '*OpenSSL*'
    filter_0:
        source.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/recon.js
    domain: net
    technique: T1592
    technique_name: Gather Victim Host Information
    original_query: |-
        NOT source.ip: $MPNET
        AND http.response.status_code: (
          200 OR 301 OR 302
          OR 400 OR 403 OR 404
        )
        AND http.response.headers.server: (
          *Apache* OR *nginx*
          OR *Microsoft-IIS*
          OR *PHP* OR *Tomcat*
          OR *Jetty* OR *Werkzeug*
          OR *OpenSSL*
        )
    arkime: |-
        ip.src != $MPNET
        && protocols == http
        && http.statuscode == [200, 301, 302, 400, 403, 404]
    suricata: |-
        alert http $HTTP_SERVERS any
          -> $EXTERNAL_NET any
          (msg:"RECON T1592.002 HTTP Server
            header software version
            disclosure";
          flow:established,from_server;
          pcre:"/Server\s*:\s*[^\r\n]*(
            Apache\/|nginx\/|
            Microsoft-IIS\/|PHP\/|
            Tomcat\/|Jetty\/|
            Werkzeug\/|OpenSSL\/)/ix";
          http.header;
          classtype:policy-violation;
          sid:9159209; rev:1;)
    requires_variables:
        - $MPNET
    actors:
        - APT40
        - APT33
        - Multi
    analyst_notes: 'HTTP Server headers advertising specific versions (Apache/2.4.49, nginx/1.18.0, PHP/7.4.3) give adversaries a precise CVE selection guide. This rule fires on egress - your own servers advertising their stack. Remediate: Apache: ServerTokens Prod; nginx: server_tokens off. Also watch X-Powered-By, X-Generator, X-AspNet-Version headers.'
