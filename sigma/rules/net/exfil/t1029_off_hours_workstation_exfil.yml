# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/exfil.js
title: T1029 - Off-Hours Workstation Exfil
id: 842b76e4-410b-5a80-a9c3-dd3cc5ca2b61
status: experimental
description: 'Outbound large transfer at off-hours from workstation source NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1029
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.exfiltration
    - attack.t1029
    - attack.g0016
    - attack.g0096
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $WORKSTATIONS
        source.bytes|gt: 104857600
        '@timestamp.hour':
            - 0
            - 1
            - 2
            - 3
            - 4
            - 5
            - 22
            - 23
    filter_0:
        destination.ip: $MPNET
    condition: selection and not filter_0
falsepositives:
    - Unknown - baseline before enabling
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/exfil.js
    domain: net
    technique: T1029
    technique_name: Scheduled Transfer
    original_query: |-
        source.ip: $WORKSTATIONS
        AND destination.ip: NOT $MPNET
        AND source.bytes > 104857600
        AND @timestamp.hour: (0 OR 1 OR 2 OR 3 OR 4 OR 5 OR 22 OR 23)
    suricata: |-
        [Time-of-day filtering is typically
        done in SIEM correlation rules
        rather than Suricata signatures.
        Suricata can detect the volume,
        but time-context filtering happens
        at log analysis layer in Kibana
        or Splunk based on @timestamp.]
        SIEM-side detection
    requires_variables:
        - $MPNET
        - $WORKSTATIONS
    dropped_clauses:
        - 'numeric-comparison (source.bytes > 104857600): emitted as |gt modifier, backend support varies'
    actors:
        - APT29
        - APT41
        - Multi
    analyst_notes: 'Workstations should not be uploading 100MB+ to external destinations at 03:00 AM on a Tuesday. Servers might (backups, automated reports) but should be tightly allowlisted. Detection: aggregate outbound volume per workstation source per hour, alert when off-hours volume exceeds threshold. Build $WORKSTATIONS to distinguish from servers/cloud-connected systems with legitimate scheduled traffic. Adversaries deliberately schedule exfil for off-hours when SOC coverage is reduced and user activity is low (less noise to hide in but also less observation). Ironically, the off-hours scheduling intended for stealth becomes a detection signal in environments with proper time-aware monitoring. Pair with destination reputation, day-of-week (weekend off-hours are even more suspicious for non-weekend-worker users), and user activity correlation (no recent keyboard/mouse activity per EDR + large outbound transfer = high confidence).'
