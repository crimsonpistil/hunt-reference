# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/credential.js
title: T1003.001 - LSASS Dump Exfiltration
id: 0485f49c-920b-54b8-a15c-d87e6db3b10b
status: experimental
description: 'SMB write of memory dump files - exfiltrating LSASS dumps NOTE: partial conversion - one or more source clauses could not be expressed in stock Sigma. See tonk.dropped_clauses.'
references:
    - MITRE ATT&CK T1003.001
    - CISA AA23-320A
    - CISA AA24-038A
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.credential-access
    - attack.t1003.001
    - attack.g1017
    - attack.g1015
    - attack.g0016
    - attack.g0096
logsource:
    category: network_connection
    product: zeek
detection:
    selection:
        source.ip: $MPNET
        destination.port: 445
        file.name:
            - '*lsass*.dmp*'
            - '*.dmp'
            - '*memory.dmp'
        file.size|gt: 10485760
    condition: selection
falsepositives:
    - legitimate crash dumps being moved by IT, Windows Error Reporting (WER) artifacts - both should originate from sanctioned crash-collection servers ($CRASH_DUMP_SERVERS allowlist)
level: medium
tonk:
    fidelity: partial
    source_file: net/js/data/credential.js
    domain: net
    technique: T1003.001
    technique_name: 'OS Credential Dumping: LSASS Memory'
    original_query: |-
        source.ip: $MPNET
        AND destination.port: 445
        AND file.name: (*lsass*.dmp* OR *.dmp OR *memory.dmp)
        AND file.size > 10485760
    suricata: |-
        alert tcp $HOME_NET any
          -> $HOME_NET 445
          (msg:"TA0006 T1003.001 SMB write
            of memory dump file LSASS
            exfiltration";
          flow:established,to_server;
          pcre:"/(lsass\.dmp|memory\.dmp|
            \.dmp)/i";
          classtype:trojan-activity;
          sid:9100301; rev:1;)
    requires_variables:
        - $MPNET
    dropped_clauses:
        - 'numeric-comparison (file.size > 10485760): emitted as |gt modifier, backend support varies'
    actors:
        - Volt Typhoon
        - Scattered Spider
        - APT29
        - APT41
        - Ransomware
        - Multi
    analyst_notes: 'If you ship Zeek logs to Kibana, you can sharpen this KQL by adding zeek.smb_files.action or zeek.smb_cmd.command filters (e.g. "SMB_FILE_READ" / "SMB_FILE_WRITE" / "get_dfs_referral") - the baseline KQL above falls back to port/protocol since Zeek shipping is not assumed. LSASS memory dumping (Mimikatz, ProcDump, Task Manager, comsvcs.dll MiniDump, custom tools) produces a .dmp file (typically 50-150MB for LSASS). Adversaries then exfiltrate the dump for offline credential extraction with pypykatz or Mimikatz. Network signal at the exfiltration step: SMB write of large .dmp files. The 10MB minimum threshold filters out small unrelated dumps. False positives: legitimate crash dumps being moved by IT, Windows Error Reporting (WER) artifacts - both should originate from sanctioned crash-collection servers ($CRASH_DUMP_SERVERS allowlist). After exclusions, large .dmp file movement is typically credential exfiltration. Pair with subsequent activity that suggests offline credential cracking: outbound traffic to attacker infrastructure, downloads of Mimikatz tools.'
