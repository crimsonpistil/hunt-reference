# Generated by TONK sigma-export.py - do not edit by hand.
# Source of truth: net/js/data/cloud.js
title: T1537 - Resource Hijack / Unusual Region Spin-Up
id: 9e977c4e-f416-5ed6-8f2f-006761ff7aac
status: experimental
description: Compute launched in an unused region or at anomalous scale - resource hijack / staging or instance count / size far above. baseline for the principal.
references:
    - MITRE ATT&CK T1537
    - T1496
author: TONK Hunt Reference (6b74.dev)
date: 2026/07/21
tags:
    - attack.exfiltration
    - attack.t1537
    - attack.g0059
logsource:
    product: aws
    service: cloudtrail
detection:
    selection:
        event.dataset: aws.cloudtrail
        event.action:
            - RunInstances
            - CreateCluster
            - StartInstances
    filter_0:
        cloud.region: $KNOWN_REGIONS
    condition: selection and not filter_0
falsepositives:
    - legitimate expansion into a new region (should have change records), DR drills, data-science workloads that genuinely need big instances - baseline per team
level: medium
tonk:
    fidelity: full
    source_file: net/js/data/cloud.js
    domain: net
    technique: T1537
    technique_name: Transfer Data to Cloud Account
    original_query: |-
        event.dataset: "aws.cloudtrail"
        AND event.action: ("RunInstances" OR "CreateCluster"
          OR "StartInstances")
        AND NOT cloud.region: $KNOWN_REGIONS
    suricata: |-
        [Cloud-side detection only for the launch
        event. If hijacked instances then beacon
        or mine, THAT egress may cross sensors -
        see C2 (TA0011) and the cryptomining
        SNI/pool patterns there.]
    requires_variables:
        - $KNOWN_REGIONS
    actors:
        - Multi
        - APT35
    analyst_notes: 'Two motives share one signature: (1) resource hijack for cryptomining/proxyware - attackers spin up expensive GPU/compute in regions you don''t use, hoping it hides in the billing noise; (2) staging - attackers stand up their own instances inside your account as a foothold or exfil relay. Detection: RunInstances / CreateCluster in a region outside $KNOWN_REGIONS, or instance type/count far above the launching principal''s baseline (a dev who launches t3.micros suddenly launching GPU instances). FALSE POSITIVES: legitimate expansion into a new region (should have change records), DR drills, data-science workloads that genuinely need big instances - baseline per team. Pair with downstream signals: hijacked miners connect to mining pools (catch on the network side via SNI/pool ports in C2), and staging instances make outbound connections that don''t fit normal patterns. Cost anomaly alerts from billing are a useful independent corroborator.'
